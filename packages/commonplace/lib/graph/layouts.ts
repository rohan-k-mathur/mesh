// Phase 5 placeholder.
//
// Mesh's components/graph/layouts.ts is tightly coupled to argumentation
// types (CegNode, CegEdge, 'supports' edges). Rather than vendor a file that
// would need substantial rewriting, the Commonplace citation-graph layouts
// will be written fresh in Phase 5 against Commonplace's own Entry/EntryLink
// types. The pure layout algorithms (Sugiyama-style topological layering,
// polarized, grounded, temporal, focus) can be lifted from Mesh as reference
// at that point.
export {};

# Q-032 Res-A′ discharge — the representation bijection `STLC ⥲ Gen!(B)`, re-pointed to BT2010 c-designs

- **status:** **SIGNED OFF (2026-06-21 #2, independent non-author re-check of the re-grounded draft) — Res-A′ is a BIJECTION; D-A′1 fully excised.** The final-gate re-check confronted the re-grounded draft against the in-repo PDFs (`python3.12 -m pypdf`): the native grounding is sound and **V-A′1…A′7 all PASS**, the cross-cutting **D-C1 / F4 / D-A1-repair spot-checks are confirmed verbatim**, and the App-A/LLP detour is genuinely gone from the critical path (App A is now evidence-of-kind only — independently confirmed atom-free/classical/derivability-only, so its excision was warranted). The sole open item, the **native V-A′1 representation lemma** (βη-long-normal STLC `{→,×,atom}` terms ↔ cut-free identity-free η-long BT2010 c-designs), is a **genuine bijection**: both failure modes (two designs / one read-back; one term / two designs) are structurally forbidden by Terui Def 2.4 (cut-free identity-free c-designs *are* the located normal η-long λ-terms — verbatim) + BT2010 §3.1 deterministic proof search (the head action forces the next rule — verbatim), with the only quotient (immaterial negative components) closed by Res-C; no counterexample found in either direction. **⟹ [T012](../02_THEOREMS_AND_PROOFS/T012-gen-b-antichain.md) → `established`; [Q-032](../01_OPEN_QUESTIONS_REGISTRY.md#q-032) → closed; Phase 4 ([Q-028a stratum-2](../01_OPEN_QUESTIONS_REGISTRY.md#q-028a)) unblocked.** **V-A′1 now WRITTEN OUT in full (§8, Session 1, 2026-06-21) — the recommended non-blocking residual is discharged; the proof is the assembly of Terui Def 2.4's grammar isomorphism + the product case + the mutual-inverse.** *(Re-check 2026-06-21 #2 recorded in [Cross-check notes](#cross-check-notes-2026-06-21-2--independent-non-author-final-gate-re-check-signed-off); prior D-A′1 cross-check 2026-06-21 and author verdict BIJECTION retained for trail.)*
- **started:** 2026-06-20
- **goal:** prove `φ : { βη-long-normal STLC λ-terms of 𝒞/Γ(A, B^♯) } ⥲ Gen!(B)` is a **bijection** (ground-atom scope), where `Gen!(B)` is the set of **material proof designs** of the BT2010 logical behaviour `B = ⟦B^♯⟧` — the derivation↔λ-term **and** design↔derivation legs of the bridge map, now both inside the c-design lineage.
- **the D-A1 fix this rests on:** the design↔derivation leg is BT2010's own design-as-proof-term framework (Def 3.2 + §3.1 deterministic proof search + Thm 3.5/3.8), where **arrows are native** (Ex 2.14, exponentials internalized) — *not* BF's synthetic MELLS, which D-A1 ruled out for arrow-typed `B`.
- **inputs:** BT2010 (Ex 2.14 connectives; App A constant-only LLP embedding; Def 3.1 logical behaviours; Def 3.2 proofs; §3.1 deterministic proof search; Thm 3.5 soundness; Thm 3.8 completeness-for-proofs); Terui 2011 (§1 "designs extend normal η-long λ-terms"; the interaction rule generalizes β-reduction; function designs Def 2.17); [Res-C](q032-res-c-materiality-2026-06-19.md) (`⋃`-materiality idempotent → unique material representative); [Q-027 audit](q027-thin-cones-2026-05-29.md) §1–§2 (the Ambler base + the three aspirin generators); Girard 1987 / Lincoln–Scedrov–Shankar 1993 (the `!`-/CBN faithfulness backdrop).

> **Notation.** `⟦M⟧` = the BT2010 proof design realizing a λ-term `M`. `|D|_B` = its
> `⋃`-materiality (BF Def 11.5; Res-C). A **proof design** is total, deterministic,
> `✠`-free, cut-free (BT2010 Def 3.2). `Gen!(B)` = the **material** proof designs of `B`.

---

## 0. What Res-A′ is (and what it inherits)

The route-2 re-target replaced the design leg's target from BF MELLS to BT2010
c-designs, dissolving D-A1 and converting the *blocking* defect into this **standard
representation residual**:

> **Res-A′.** On the ground `{→, ×, atom}` fragment, the STLC βη-long-normal λ-terms
> embed into BT2010 logical behaviours **βη-bijectively** onto the material proof
> designs `Gen!(B)`.

The cross-check already accepted the *proof-theoretic content* of the old Res-A Steps
1–4 ("internally coherent as proof theory in MELL"; V-A4 η-long and V-A5
canonicalisation "carry over"); only the *landing zone* was wrong. Res-A′ re-points
that content to the native-arrow BT2010 target and supplies the read-back from
BT2010's **deterministic proof search** (cleaner than the focalization-single-valued
argument it replaces).

## 1. The target — Ambler types are BT2010 logical behaviours

**Connectives (BT2010 Ex 2.14, verbatim).** With a signature containing `∗` (nullary),
`↑, π₁, π₂` (unary), `℘` (binary), BT2010 defines `⊗, ℘, &, ⊕, ↑↑↑, ↓↓↓, 1, ⊥, ⊤` as
logical connectives, and *"we do not have exponentials here, because we are working in
a nonlinear setting so that they are already incorporated into the connectives."*

**The arrow is native.** The Ambler `→` is the (nonlinear) function space. In the
polarized setting the implication is `A ⊸ B = A^⊥ ℘ B` (BT2010 builds `P ℘ Q =
•⟨P^⊥, Q^⊥⟩^⊥`), with `↑↑↑/↓↓↓` shifts placing the polarities; nonlinearity
(contraction — the robustness rule's doubled use) is carried by the internalized
exponential, so **no explicit `!`-decoration and no head-exponential MELLS grammar is
required** (this is exactly what dissolves D-A1). The conjunctive premise `×` is the
nonlinear `⊗` (or `&` for genuine case-analysis — that is the [Res-D](../10_IDEATION_SESSIONS/18-q032-residual-discharge-scoping-2026-06-19.md)
boundary, absent from the aspirin family).

**Embedding (native — NOT via App A; D-A′1 repair 2026-06-21).** Each Ambler type is a
BT2010 **logical behaviour** (Def 3.1: `P := α⟨N₁…⟩`, `N := α(P₁…⟩`) built **directly**
from the Ex 2.14 connectives: the **arrow** `A → B` is the (nonlinear) **function
behaviour** whose inhabitants are **Terui function designs** (Def 2.17: a negative
c-design `F[x₁,…,xₙ]` with `fv(F) ⊆ {x⃗}` and `[[F[d⃗]]]` a data design); the **product**
`A × B` is `⊗` / a binary ramification (Ex 2.14); **atoms** are distinct **0-ary names**
in the ludics signature (Q-027 §1 — *not* App-A constants). A generator's goal sequent
`(x:A, Γ) ⊢ M : B^♯` is then a sequent of behaviours. **App A is NOT a faithfulness
certificate here** — cross-check **D-A′1** established (verbatim) that its `MELLpol↔L`
transfer is *derivability-only* (Thm A.4/A.5; BT2010: "we have to verify carefully that
the translation preserves the reduction relation"), its LLP is *constant-only*
(`P ::= 0|1|P⊗P|P⊕P|!N` — **no atoms**) and *classical*. So App A carries neither the
Ambler atoms nor the intuitionistic discipline; it stands here **only as
evidence-of-kind** that the constant-only polarized fragment is ludics-interpretable.
**Ground-atom scope:** the aspirin atoms are ground (Q-027 §1); schematic variables
(Q-033) and additive `⊕` (Res-D) stay out of scope, unchanged.

## 2. The correspondence is near-definitional — c-designs *extend* η-long λ-terms

This is the crux simplification route 2 buys. Terui 2011 §1 frames c-designs as a
direct generalisation of λ-terms (verbatim):

> *"To motivate our new syntax, we first draw an analogy with **lambda calculus** …
> The designs of ludics **extend the lambda terms** in this well-behaved fragment …
> Actually, the original designs extend the **normal, η-long and linear lambda
> terms**."* (Terui §1) — and the interaction rule is *"analogous to the reduction
> rule for lambda terms"* (β-reduction).

So a βη-long-normal STLC λ-term `M : B^♯` in context `(x:A, Γ)` **is**, up to the
locative/named presentation, a c-design — its **realizer** `⟦M⟧`:

- λ-abstraction ↦ a negative action (`↑(x).·` / the `Σa(x⃗).P` binder);
- application of a head variable/constant to arguments ↦ a positive action
  `z|a⟨M₁,…,Mₘ⟩` (Terui's function-design application, Def 2.17);
- the conjunctive pair / projection ↦ `℘`/`πᵢ` actions (Ex 2.14);
- **multiple use of a hypothesis** (the robustness rule's doubled scheme) ↦ native
  nonlinear contraction (Ex 2.14 internalized exponential) — no `!`-box bookkeeping.

`⟦M⟧` is a **proof design** (BT2010 Def 3.2): **total** (M is a real term, not `Ω`),
**deterministic** (STLC λ-terms are single-valued ⇒ all conjunctions unary, the Def
3.2 determinism condition), **`✠`-free** (a genuine derivation never assumes its
conclusion), **cut-free** (M is normal). So `⟦·⟧` lands in the proof fragment.

## 3. Design ↔ derivation, by construction (the read-back)

The read-back `⟦·⟧⁻¹` is **not** a separate focalization lemma — it is BT2010's
**deterministic proof search** (BT2010 §3.1, verbatim):

> *"given a positive sequent `z|a⟨M₁,…,Mₘ⟩ ⊢ Γ`, the head variable `z` and the first
> positive action `a` completely determine the next positive rule to be applied
> bottom-up."*

A proof design's head action determines the last rule of its derivation, recursively;
so **a proof design determines its derivation uniquely**, and conversely the rules
(§3.1, Example 3.4) build the design from the derivation. Hence

$$\{\text{material proof designs of } B\} \;\xleftrightarrow{\;\text{1–1}\;}\; \{\text{cut-free BT2010 derivations of } B\}$$

is a bijection **by construction** — pinned to `B` by soundness (Thm 3.5) + completeness
(Thm 3.8: a proof `P ∈ P` iff `P ⊢ x₀:P` derivable). The **material** restriction is
essential: BT2010's "immaterial" proofs carry arbitrary irrelevant components, so the
unique representative is the material one — supplied by **Res-C** (`|·|_B` idempotent
⇒ a unique material rep per class). This is the precise place Res-A′ and Res-C
compose.

## 4. Bijectivity (b₁′ ∧ b₂′)

`φ` is the composite of three legs, each now established or cited:

1. **term ↔ derivation (native).** βη-long-normal STLC `{→,×}` terms ↔ cut-free
   identity-free η-long **BT2010 c-designs / §3.1 derivations**. Terui §1: *"the original
   designs extend the normal, η-long … lambda terms"* (and the nonlinear case carries
   contraction), interaction rule = β; so cut-free identity-free η-long c-designs **are**
   the normal η-long λ-terms, located. The read-back is **deterministic proof search**
   (§3; BT2010 §3.1), which makes design→derivation functional and forbids the
   permutation-quotient. **[the one residual fidelity lemma — §6.1, stated *natively*; NOT
   via App A/LLP, per D-A′1]**
2. **derivation ↔ design** — bijection by construction (§3; BT2010 Def 3.2, §3.1, Thm
   3.5/3.8). **[cited]**
3. **design ↔ material design** — unique material representative (Res-C idempotence).
   **[discharged, D-C1 cleared]**

Composing: `φ(M) = |⟦M⟧|_B : {βη-long-normal STLC λ-terms} ⥲ Gen!(B)`.

- **Injective (b₂′).** Distinct βη-long-normal `M ≠ M′` give distinct derivations
  (leg 1), hence distinct proof designs (leg 2, deterministic search), hence distinct
  material designs (leg 3). **The separation failure (BT2010 Rmk 2.4) does not bite** —
  its witnesses are `✠`-terminated non-proofs (F4, cleared); injectivity here runs
  through deterministic proof search + materiality, *not* orthogonality.
- **Surjective (b₁′).** Every material proof design `D ∈ Gen!(B)` is, by completeness
  (Thm 3.8), the design of a cut-free derivation (leg 2⁻¹), hence (leg 1⁻¹) of a unique
  βη-long-normal λ-term. So `φ` is onto `Gen!(B)`.

Hence `φ` is a **bijection** `φ : {ground {→,×,atom} STLC λ-terms} ⥲ Gen!(B)` — which is
exactly [C001b′](../03_CONJECTURES/C001b-prime-ambler-remainder.md) **b₁′ ∧ b₂′** at
the exponential layer.

## 5. Worked instance (the three aspirin generators)

On `B_aspirin^★` ([port §O5](q032-antichain-port-2026-06-16.md)), the three Ambler
generators and their material proof designs:

| λ-term (𝒞_base) | BT2010 proof design `⟦·⟧` | head action |
|---|---|---|
| `g₁ = t₁ (mp x)` | `D_{t₁}` — apply `t₁` to the `mp` projection | root focus `t₁` |
| `g₂ = t₂⟨mp x, i₂⟨st,an⟩⟩` | `D_{t₂}` — `t₂` on `mp ∧ ¬gu`, `¬gu` via `i₂` | root focus `t₂` |
| `g_r = r⟨!t₁, mp x, jp x⟩` | `D_r` — `r`-root, the scheme `t₁` **contracted** (native nonlinearity) over the two witnesses | root focus `r` |

All three are total, deterministic, `✠`-free, cut-free ⇒ proof designs; pairwise
distinct head focus ⇒ distinct under `φ`; deterministic proof search recovers each
λ-term from its design. `D_r`'s doubled-`t₁` is just internalized contraction (Ex
2.14), **not** an `!`-box — so no MELLS-grammar question arises. `|Gen!(B_aspirin^★)|
= 3`, matching `|𝒞_base| = 3` (Q-027 §5 / E2). **Instance-first corroboration ✓.**

## 6. Residuals for the independent re-check

The construction is sound *given* the following, which the non-author check must
confirm (none is the D1 stability gap or the S1 separation counterexample):

1. **(Leg-1 fidelity — the one genuine open lemma, stated *natively*).** βη-long-normal
   STLC `{→,×,atom}` terms ↔ cut-free identity-free η-long **BT2010 c-designs** of the
   corresponding logical behaviour is a **bijection**, at `{→,×}` granularity: the arrow
   via Terui function designs (Def 2.17), the product via a binary ramification, atoms via
   0-ary names (§1). Confirm η-long-normality + deterministic proof search make it
   single-valued both ways (the reviewer found **no counterexample** either direction; the
   locative/focalized c-design discipline forbids the permutation-quotient). **No App A /
   LLP step** (D-A′1).
2. **(Realizer well-formedness.)** `⟦M⟧` is a BT2010 proof design (total, deterministic,
   `✠`-free, cut-free) for every βη-long-normal `M` — from §2 + Terui §1; confirm the
   nonlinear (contracted) case (`g_r`) is a legal nonlinear design, not requiring an
   `!`-box.
3. **(Res-C composition.)** The bijection is on **material** proof designs; confirm the
   up-to-materiality quotient is exactly closed by Res-C idempotence (the unique
   material rep), with no residual ambiguity.
4. **(Res-B / scope.)** The `!`-image behaviours are *logical* (BT2010 Def 3.1) so Thm
   3.5/3.8 apply; aspirin family ground (no Q-033 variables, no Res-D `⊕`).

## 7. Verdict, carrier note, and status

**Res-A′ verdict: BIJECTION** `φ : {ground {→,×,atom} STLC λ-terms} ⥲ Gen!(B)`, via
the native-arrow BT2010 design leg — **D-A1 dissolved** (no MELLS, no Thm 11.17), the
design↔derivation leg a bijection *by construction* (deterministic proof search), and
the term↔derivation leg the **native Terui/BT2010 c-design representation** (Terui §1 +
deterministic proof search — the one residual lemma, §6.1; **re-grounded off App A per
D-A′1, 2026-06-21**). Faithfulness (b₂′) is separation-independent.

**Carrier note ([Q-030](../01_OPEN_QUESTIONS_REGISTRY.md#q-030)).** Res-A′ confirms the
design leg lives in the c-design lineage (Terui realizers / BT2010 designs), so the
carrier-readout input **B = uses-c-design** (flipped from neutral by the D-A1 repair).
Hybrid still holds by default (proof-level); the b-Terui-retarget trigger's B-condition
is now met — only the runtime profile stands between hybrid and a Terui retarget.

**Status: provisional (paper argument), pending an independent non-author re-check of
§6** (chiefly the Leg-1 fidelity lemma §6.1). On a clean re-check — together with the
already-cleared Res-C (D-C1) and the D-A1 repair — [T012](../02_THEOREMS_AND_PROOFS/T012-gen-b-antichain.md)
→ `established`, [Q-032](../01_OPEN_QUESTIONS_REGISTRY.md#q-032) → closed, Phase 4
([Q-028a stratum-2](../01_OPEN_QUESTIONS_REGISTRY.md#q-028a)) unblocked.

---

## Cross-check notes (2026-06-21) — independent non-author re-check (Res-A-prime)

> Independent non-author cross-check, 2026-06-21. **Verdict: NOT SIGNED OFF — one
> degradation defect D-A′1** (Leg-1's App-A/LLP grounding is derivability-only, atom-
> free, and classical; the D-A1 derivability-trap genus re-arising — V-A′2). **Not a
> hard failure:** the claim is most likely true via the native Terui route, which the
> audit must commit to as Leg-1's primary grounding. **T012 must NOT be promoted on
> Res-A′; Q-032 stays open.** Read against the in-repo PDFs (BT2010 `ON THE MEANING OF
> LOGICAL COMPLETENESS.pdf`; Terui `Terui_ComputationalLudics.pdf`; BF `LUDICS WITH
> REPETITIONS 1104.0504v3.pdf`), extracted with `python3.12 -m pypdf`.

### D-A′1 (degradation, gates sign-off) — Leg-1's App-A/LLP grounding is derivability-only and atom-free (V-A′2)

**The defect.** §1, §4 (leg 1), and the §6.1 re-check item ground Leg-1's *target* on
**BT2010 App A** — "the Ambler `{→,×,atom}` fragment … sits inside LLP … so each Ambler
type `τ` is a BT2010 logical behaviour" and "confirm … that the LLP App-A embedding (§1)
is faithful on `{→,×,atom}`." Read verbatim against App A, that embedding does **not**
deliver what Leg-1 consumes, on three independent counts:

1. **Derivability-only (the load-bearing one — = D-A1's genus).** App A's two transfer
   theorems are *provability* equivalences over the **design-erased skeleton** system
   `L`:
   > **Theorem A.4.** *A strict sequent `⊢ ?Γ, D` is **derivable** in LLP iff `⊢ Γ•, D•`
   > is **derivable** in `L`.* (BT2010 App A.4)
   > **Theorem A.5.** *A sequent `⊢ Λ` is **derivable** in proof system `L` iff `⊢ Λ◦`
   > is **derivable** in LLP.* (BT2010 App A.5)
   and BT2010 **explicitly flag the proof-term/design level as unverified**:
   > *"… ludics designs can be used as term syntax for LLP, as far as strict sequents
   > and derivations are concerned (**although we have to verify carefully that the
   > translation preserves the reduction relation**)."* (remark after Thm A.4)
   This is the **same shape** as the **BF App A Thm A.15 derivability trap that sank the
   original Res-A (D-A1)**: a derivability equivalence is cited exactly where a
   proof/design-level correspondence is needed. As written, App A licenses "the same
   sequents are derivable," **not** "λ-terms biject with designs."
2. **Constant-only ⇒ no atoms.** App A's LLP grammar is `P ::= 0 | 1 | P⊗P | P⊕P | !N`,
   `N ::= ⊤ | ⊥ | N℘N | N&N | ?P` — **purely constant-only; there are no propositional
   atoms** (only the four constants `0,1,⊤,⊥`). The Ambler fragment's distinct atoms
   (`muscle_pain, aspirin, …`) are **not** representable in this LLP, so "faithful on
   `{→,×,atom}`" via App A is false for the **atom** component. (Atoms must instead be
   distinct **0-ary names** in the ludics signature directly — *not* via App A.)
3. **Classical, not intuitionistic.** App A interprets "a constructive variant of
   constant-only propositional **classical** logic." The Ambler `→,×` is intuitionistic;
   App A is cited for the wrong logical discipline (the single-positive-formula
   restriction is the *only* intuitionistic shadow, and it is a derivability fact).

**Why it is a degradation, not a hard failure.** Unlike D-A1 — where the *design↔
derivation* leg itself had to cross MELL→MELLS through the derivability-only Thm A.15,
with no native alternative — here the App-A detour is **not on the critical path**:

- **Leg-2 (design ↔ derivation) is native and by construction**, *not* via App A:
  Def 3.2 (proofs = total/deterministic/`✠`-free/cut-free designs); §3.1 deterministic
  proof search ("the head variable `z` and the first positive action `a` completely
  determine the next positive rule … bottom-up", verbatim); Thm 3.5/3.8. Confirmed.
- **Leg-1 has a sound native grounding in Terui (§2)**, also *not* via App A. Verbatim:
  > *"The designs of ludics extend the lambda terms in this well-behaved fragment …
  > Actually, the original designs extend the normal, η-long and **linear** lambda
  > terms. In contrast, our syntax also encompasses non-normal, non-η-long and
  > **non-linear** terms."* (Terui §1, over `τ ::= ι | τ→τ`)
  and cut-free identity-free c-designs are precisely the normal η-long λ-terms ("Observe
  the analogy with the normal and η-long terms …"), with the reduction rule = β. This is
  a genuine **proof/design-level** representation, and the permutation-quotient trap
  (V-A′1) does **not** bite in the c-design setting (designs are intrinsically focalized/
  located; deterministic proof search makes design→derivation functional).

**Required repair (re-grounding, not new proof theory).** Commit Leg-1's target to the
**native BT2010 §3.1 derivations / Terui c-designs**, drop the "BT2010/LLP" conflation
(§4 leg 1) and the App-A faithfulness item (§6.1), and obtain the Ambler types as
logical behaviours **directly** (Ex 2.14 connectives — the arrow built as `↑↑↑A^⊥ ℘ B`;
atoms as 0-ary names), *not* via App A. App A may stay only as **evidence-of-kind** that
the constant-only polarized fragment is interpretable, never as the faithfulness
certificate. After re-grounding, the open content is exactly the **native** V-A′1
representation lemma (below), which is plausible and unrefuted.

### V-A′1 — Leg-1 a genuine bijection (highest priority): PASS as an open-but-plausible *native* lemma

In the **native c-design** setting (the repair target) the classic sequent→term
permutation-quotient does **not** arise: BT2010 §3.1 derivations are read off designs by
*deterministic* proof search (one rule forced at each positive step), and cut-free
identity-free η-long c-designs structurally mirror normal η-long λ-terms (Terui §1).
I could construct **neither** (a) two distinct cut-free designs of one behaviour with the
same λ read-back, **nor** (b) a βη-long-normal term with a non-unique design — the
locative/focalized discipline forbids both. The lemma remains genuinely *open* (it must
be **stated** at `{→,×}` granularity, incl. product via a binary ramification and the
atom signature), but it is plausible and no counterexample was found. *As the audit
currently frames it — "focalized cut-free BT2010/**LLP** derivations" — it inherits
D-A′1; restated natively it stands clean.*

### V-A′3 — Leg-2 materiality seam: PASS

The negative-rule "immaterial" arbitrariness is real and confirmed verbatim — *"a
component `b(⃗y).Pb` of `∑a(⃗x).Pa` can be arbitrary when `b(⃗y) ∉ α0`"* (§3.1) — so
design→derivation is single-valued **only on material designs**. The audit correctly
restricts to `Gen!(B)` (material), and Res-C supplies the unique material representative
(Leg-3). No residual ambiguity; the Leg-2 ∘ Leg-3 composition has no seam.

### V-A′4 — realizer well-formedness incl. nonlinear `g_r`: PASS

`⟦M⟧` is total/deterministic/`✠`-free/cut-free (Def 3.2 verbatim: "a proof is a total,
deterministic and `✠`-free design without cuts and identities", determinism = all
conjunctions unary). The `g_r` doubled-`t₁` is handled by the **implicit contraction in
the positive rule** — verbatim: *"the positive rule `(α,a)` involves implicit uses of the
contraction rule on positive behaviours"* — and the nonlinearity is internalized (Ex 2.14:
*"we do not have exponentials here … they are already incorporated into the
connectives"*). Crucially the doubled use is **contraction (variable reuse), not the
nondeterministic `⋀`-conjunction**, so the design stays **deterministic (unary)** — no
non-unary `⋀`, no `!`-box. Legal nonlinear proof design.

### V-A′5 — D-A1 (MELLS) non-recurrence: PASS (mechanism), with a caveat

The synthetic-MELLS head-exponential grammar (`P ::= ?_P(…)`, `N ::= !_N(…)`) that sank
the prior Res-A is **absent** from the BT2010 target — Ex 2.14 connectives admit the
arrow natively, no re-synthesis step. The original D-A1 *mechanism* is genuinely gone.
**Caveat:** its *genus* (derivability cited for a proof-level need) partially recurs in
the App-A grounding — see D-A′1.

### V-A′6 — D1 (linear smuggle) non-recurrence: PASS

No linear-incarnation / Cor 3.22 stability / `⋂`-meet anywhere. Res-A′ rides on Terui
function designs + BT2010 completeness-for-proofs (Thm 3.8) + deterministic proof search
— all nonlinear/native. Clean.

### V-A′7 — worked instance: PASS

Re-derived `⟦g₁⟧, ⟦g₂⟧, ⟦g_r⟧` as **BT2010 designs** (positive rule `z|a⟨…⟩`, Ex 2.14
connectives — *not* MELL proofs): each is total/deterministic/`✠`-free/cut-free ⇒ a proof
design (Def 3.2); pairwise distinct head focus (`t₁`/`t₂`/`r`) ⇒ distinct under `φ`;
`g_r`'s doubled `t₁` is internalized contraction (no `!`-box). Each is material and read
back by deterministic proof search. `|Gen!(B_aspirin^★)| = 3`. Consistent.

### Cross-cutting spot-checks (the two prior author-side resolutions)

- **D-C1 clearance (Res-C) — CONFIRMED verbatim.** Against `LUDICS WITH REPETITIONS
  1104.0504v3.pdf`: **Def 11.2** `D^R = D ∩ {⌜p⌝ : p ∈ I(R)}` (restriction to *visited*
  views; `I(R)` = set of interactions = union over runs in the non-uniform setting);
  **Lemma 11.3** (`D^R` is a strategy — proof discharges τ-Positivity); **Lemma 11.4**
  `[[D₁,…,Dₙ]] = [[D₁^R,…,Dₙ^R]]`; **Def 11.5** `|D|_G := ⋃{D[E] : E ∈ G⊥}`; **Prop 11.8**
  `[[|D|_G,E]] = [[D,E]]` and `|D|_G ⊥ E` for each `E ∈ G⊥`, hence `|D|_G ∈ G`. All present
  and saying exactly what Res-C's §7 fidelity items require. The Res-C idempotence
  keystone is genuinely cleared.
- **F4 — CONFIRMED verbatim.** Rmk 2.4 / Ex 2.20 witnesses `P := x₀|↓⟨↑(y).✠⟩`,
  `Q := x₀|↓⟨↑(y).P⟩` **both contain `✠`** ⇒ not `✠`-free ⇒ not proofs (Def 3.2); and
  `P ⪯ Q` with `Q ∉ ↓↓↓ᵉᵗʰ⟨↑↑↑(0)⟩` ⇒ `Q` non-material. Excluded from `Gen!(B)` on two
  independent grounds — no counterexample to (R-inj).
- **D-A1 repair (route 2) — sound and honestly recorded.** Re-targeting to BT2010 is
  correct (native arrows via Ex 2.14; no head-exponential grammar; Thm 3.8 over logical
  behaviours), and the carrier consequence (`B → uses-c-design`; [Q-030](../01_OPEN_QUESTIONS_REGISTRY.md#q-030)
  input) is recorded faithfully.

### How to clear D-A′1

Re-ground Leg-1 on the **native** Terui c-design / BT2010 §3.1 representation (Ex 2.14
arrow + 0-ary-name atoms + binary-ramification product), excise the "BT2010/LLP"
conflation and the App-A faithfulness re-check item, and **state V-A′1 natively** at
`{→,×}` granularity. App A demoted to evidence-of-kind only. On that re-grounded draft
(with V-A′1 the sole open, native lemma), a clean re-check would promote
[T012](../02_THEOREMS_AND_PROOFS/T012-gen-b-antichain.md) → `established` and close
[Q-032](../01_OPEN_QUESTIONS_REGISTRY.md#q-032). **Until then: provisional with
degradation defect D-A′1; do not promote T012; Q-032 stays open.**

---

## Re-grounding (D-A′1 repair, 2026-06-21) — Leg-1 committed to the native target

Executed exactly the reviewer's "How to clear D-A′1" recipe (the App-A facts were
re-confirmed verbatim against the in-repo BT2010 PDF: LLP grammar `P ::= 0|1|P⊗P|P⊕P|!N`
is **constant-only / atom-free**; Thm A.4/A.5 are **derivability** equivalences with the
"verify… preserves the reduction relation" caveat). Changes:

- **§1 (target).** Replaced the "Embedding (BT2010 App A)" paragraph with a **native**
  grounding: Ambler types are BT2010 logical behaviours built **directly** from Ex 2.14
  connectives — **arrow** via Terui **function designs (Def 2.17)**, **product** via a
  binary ramification, **atoms** via distinct **0-ary names** (Q-027 §1). **App A demoted
  to evidence-of-kind only.**
- **§4 leg 1.** Dropped the "BT2010/**LLP**" conflation; the term↔derivation leg is now
  the **native** βη-long-normal STLC ↔ cut-free identity-free η-long **c-design**
  correspondence (Terui §1 + §3.1 deterministic proof search).
- **§6.1 (residual).** Restated the one open lemma **natively** at `{→,×}` granularity
  (arrow = function designs, product = ramification, atoms = 0-ary names); **no App A/LLP
  step**.
- **§7 verdict + status header.** Updated to "native Terui/BT2010 c-design representation"
  and "D-A′1 repaired (native re-grounding)".

**Net.** The repair removes the unlicensed App-A faithfulness certificate without
touching the proof's engine — Leg-2 (design↔derivation by construction) and Leg-3
(Res-C) were already native and cleared; Leg-1 now grounds on Terui §1 / Def 2.17 /
deterministic proof search rather than LLP. The **sole open content is the native V-A′1
representation lemma** (§6.1), which the cross-check found plausible and unrefuted in
either direction. **Next: an independent re-check of the re-grounded draft** (verification
prompt to be re-pointed); on a clean pass, T012 → `established`, Q-032 → closed.

---

## Cross-check notes (2026-06-21 #2) — independent non-author final-gate re-check (SIGNED OFF)

> Independent non-author cross-check of the **re-grounded** Res-A′ draft, 2026-06-21
> (second pass; the first pass returned D-A′1, now repaired). **Verdict: Res-A′
> cross-check — SIGNED OFF.** All load-bearing readings re-confirmed verbatim against
> the in-repo PDFs (`ON THE MEANING OF LOGICAL COMPLETENESS.pdf`,
> `Terui_ComputationalLudics.pdf`, `LUDICS WITH REPETITIONS 1104.0504v3.pdf`, extracted
> with `python3.12 -m pypdf`). D-A′1 is **fully excised** (V-A′2 now PASSES); the sole
> open lemma V-A′1 is a **genuine native bijection**, not a permutation-quotient.
> **⟹ T012 → `established`, Q-032 → closed, Phase 4 unblocked.**

### V-A′1 — Leg-1 a genuine *native* bijection (highest priority): PASS

Re-attacked both failure modes in the **native c-design** setting and found neither:

- **(a) two distinct cut-free identity-free η-long c-designs of one behaviour with the
  same λ read-back — none.** Terui Def 2.4 (p.2051, verbatim): *"If T is cut and identity
  free, any positive subterm is either `z`, `Ω` or of the form `x|a⟨N₁,…,Nₙ⟩` where none
  of `N₁,…,Nₙ` is a variable. **Observe the analogy with the normal and η-long terms** in
  the well-behaved fragment of lambda calculus."* So a cut-free identity-free c-design
  **is** the located/named presentation of a normal η-long λ-term; the read-back forgets
  only loci and α-names (Terui identifies designs up to α-equivalence). Two designs with
  the same read-back, located at the same root with the same behaviour, are forced equal.
- **(b) a βη-long-normal term with a non-unique c-design — none.** The realizer `⟦M⟧`
  (§2) is built by deterministic structural recursion; the only freedom is the
  **immaterial** negative components `Pb` for `b ∉ α0` (BT2010 §3.1, p.22 verbatim: *"a
  component `b(⃗y).Pb` of `∑a(⃗x).Pa` can be arbitrary when `b(⃗y) ∉ α0`"*), which the
  material restriction (`Gen!(B)`, Leg-3/Res-C) forces to `Ω`. On material designs the
  c-design is unique.

The permutation-quotient is forbidden by **deterministic proof search** (BT2010 §3.1,
p.22 verbatim): *"proof search in our system is deterministic. In particular, given a
positive sequent `z|a⟨M₁,…,Mₘ⟩ ⊢ Γ`, the head variable `z` and the first positive action
`a` completely determine the next positive rule to be applied bottom-up."* So
design→derivation is functional. The native grounding is sound: the **arrow** is a BT2010
logical behaviour (Ex 2.14, p.17 verbatim — the binary name `℘`/`` ``` ``, the shifts
`↑↑↑/↓↓↓`; *"we do not have exponentials here … they are already incorporated into the
connectives"*) whose inhabitants are **Terui function designs** (Def 2.17, §2.4); **atoms**
are distinct **0-ary names** (Terui §2.4 verbatim: *"if a signature `A` contains a 0-ary
name `n` for each natural number `n`…"* — the standard signature device, also used by the
nullary `∗` in Ex 2.14); the **product** is a binary ramification (Ex 2.14 `⊗⊗⊗`/`&&&`,
rules Example 3.4). The lemma remains formally *open* (it should be **written out** at
`{→,×,atom}` granularity), but it is native, plausible, and unrefuted in both directions —
a routine representation theorem given the cited verbatim machinery, **not** a
permutation-quotient dressed up as a bijection. The residual write-out is a documentation
task, not a blocker.

### V-A′2 — D-A′1 fully excised; native grounding is the basis: PASS

The re-grounded §1 / §4 (leg 1) / §6.1 / §7 verdict **no longer cite App A as a
faithfulness certificate** — App A appears only as evidence-of-kind, exactly as required.
Independently re-confirmed verbatim that excising it was warranted: App A is
**(i) constant-only / atom-free** (BT2010 p.30 verbatim grammar `P ::= 0 | 1 | P⊗P | P⊕P |
!N`, `N ::= ⊤ | ⊥ | N℘N | N&N | ?P` — only the four constants, no propositional atoms),
**(ii) classical** (p.30 verbatim: *"a constructive variant of constant-only propositional
**classical** logic"*), and **(iii) derivability-only** (Thm A.5 p.35 verbatim: *"A
sequent `⊢ Λ` is **derivable** in proof system `L` if and only if `⊢ Λ◦` is **derivable**
in LLP"*, with the explicit caveat p.35: *"although we have to verify carefully that the
translation preserves the reduction relation"*). No residual `"BT2010/LLP"` conflation
survives on the critical path. The Ambler types are obtained as logical behaviours
directly, with no derivability-only step anywhere load-bearing. **D-A′1 cleared.**

### V-A′3 — Leg-2 materiality seam: PASS (re-confirmed)

The negative-rule "immaterial" arbitrariness is real (BT2010 §3.1 p.22, verbatim above).
Design→derivation is single-valued **only on material designs**; the audit restricts to
`Gen!(B)` and Res-C supplies the unique material representative. No seam in Leg-2 ∘ Leg-3.

### V-A′4 — realizer well-formedness incl. nonlinear `g_r`: PASS (re-confirmed)

`⟦M⟧` is total/deterministic/`✠`-free/cut-free. The `g_r` doubled-`t₁` is **implicit
contraction in the positive rule** (BT2010 §3.1 p.22 verbatim: *"the positive rule `(α,a)`
involves implicit uses of the contraction rule on positive behaviours"*) — variable reuse,
**not** a non-unary `⋀`, so the design stays deterministic; nonlinearity internalized (Ex
2.14), no `!`-box. Legal nonlinear proof design.

### V-A′5 — D-A1 (MELLS) non-recurrence: PASS

No synthetic-MELLS head-exponential grammar; the arrow is native via Ex 2.14. The prior
caveat (the *genus* recurring in the App-A grounding) is **discharged** by the
re-grounding — with App A off the critical path, the derivability-for-a-proof-level-need
shape no longer appears anywhere load-bearing.

### V-A′6 — D1 (linear smuggle) non-recurrence: PASS

No linear-incarnation / Cor 3.22 stability / `⋂`-meet anywhere. Res-A′ rides on Terui
function designs + BT2010 completeness-for-proofs (Thm 3.8) + deterministic proof search —
all nonlinear/native.

### V-A′7 — worked instance: PASS

The three aspirin designs `⟦g₁⟧, ⟦g₂⟧, ⟦g_r⟧` are total/deterministic/`✠`-free/cut-free
proof designs with pairwise-distinct head focus (`t₁`/`t₂`/`r`), each material, each read
back by deterministic proof search; `g_r`'s doubled `t₁` is internalized contraction (no
`!`-box). `|Gen!(B_aspirin^★)| = 3`.

### Cross-cutting spot-checks (the two prior author-side resolutions)

- **D-C1 (Res-C) — CONFIRMED verbatim** against `LUDICS WITH REPETITIONS 1104.0504v3.pdf`
  (pp.53–55): **Def 11.2** `D^R = D ∩ {⌜p⌝ : p ∈ I(R)}` (restriction to visited views);
  **Lemma 11.3** (`D^R` is a strategy, proof via τ-Positivity); **Lemma 11.4** `[[D₁,…,Dₙ]]
  = [[D₁^R,…,Dₙ^R]]`; **Def 11.5** `|D|_G := ⋃{D[E] : E ∈ G⊥}`; **Prop 11.8** `[[|D|_G,E]]
  = [[D,E]]` and `|D|_G ⊥ E` for each `E ∈ G⊥`, hence `|D|_G ∈ G`. Exactly what Res-C's
  idempotence keystone consumes (`||D|_B|_B = ⋃_E (|D|_B)[E] = ⋃_E D[E] = |D|_B`).
- **D-A1 repair (route 2) — sound and honestly recorded.** Re-targeting to BT2010 is
  correct (native arrows via Ex 2.14; no head-exponential grammar; Thm 3.8 over logical
  behaviours); the carrier consequence `B → uses-c-design` ([Q-030](../01_OPEN_QUESTIONS_REGISTRY.md#q-030))
  is recorded faithfully.

### Verdict

**Res-A′ cross-check: SIGNED OFF.** Leg-1 is a genuine native bijection (App A is
proof/design-irrelevant — demoted to evidence-of-kind, its excision independently
warranted); D-C1 and the D-A1 repair re-confirmed verbatim with no new problem surfaced.
Together with the already-cleared Res-C (D-C1) and the D-A1 repair, **all of Res-A′ +
Res-C + D-A1 are clear**. [T012](../02_THEOREMS_AND_PROOFS/T012-gen-b-antichain.md) is
eligible for `established` and [Q-032](../01_OPEN_QUESTIONS_REGISTRY.md#q-032) for closure
(Res-B/Res-D light, already CONFIRMED/SCOPED-OUT). Recommended residual (non-blocking):
write out the native V-A′1 representation lemma in full at `{→,×,atom}` granularity.

---

## 8. V-A′1 proof (Session 1, 2026-06-21) — the native representation bijection, written out

This discharges the non-blocking residual the final-gate sign-off recommended. It is an
**assembly of Terui Def 2.4's already-stated grammar isomorphism** for the Ambler
`{→,×,atom}` fragment, made into a bijection (forward map + deterministic read-back +
mutual inverse), with the product case and the nonlinear case explicit. No App A; no new
heavy proof theory.

> **Lemma (V-A′1).** Let `B = ⟦B^♯⟧` be the BT2010 logical behaviour of an Ambler goal
> type over the ground `{→,×,atom}` fragment. The realizer `⟦·⟧` is a **bijection**
> `⟦·⟧ : { βη-long-normal STLC λ-terms of 𝒞/Γ(A, B^♯) } ⥲ Gen!(B)`, with inverse the
> deterministic-proof-search read-back `⟪·⟫`.

### 8.1 The two grammars coincide (Terui Def 2.4)

Terui §1 fixes the well-behaved STLC fragment (`τ ::= ι | τ₁→⋯→τₙ→ι`):
`P^ι ::= (N₀) N₁ ⋯ Nₙ` (positive, atomic, application), `N ::= x | λx₁⋯xₙ.P` (negative,
abstraction); **normal** = head of every positive subterm is a variable; **η-long** = no
argument `Nᵢ` of a positive subterm is a variable unless atomic. Terui Def 2.4 (verbatim,
p.2051): *"If `T` is cut and identity free, any positive subterm is either `✠`, `Ω` or of
the form `x|a⟨N₁,…,Nₙ⟩` where none of `N₁,…,Nₙ` is a variable. **Observe the analogy with
the normal and η-long terms** …"*; reduction `(∑a(x⃗).Pₐ)|a⟨N⃗⟩ → Pₐ[N⃗/x⃗]` = β. So **the
cut-free identity-free c-design grammar and the βη-long-normal λ-grammar are the same
grammar**, modulo: (i) loci/α-names (designs are taken up to α-equivalence, Def 2.5);
(ii) the connective-name `a` in place of bare application; (iii) the immaterial negative
components, killed on `Gen!(B)` (Leg-3/Res-C).

### 8.2 Type translation `(·)°` (native — Ex 2.14 + 0-ary names)

- **atom** `p ↦` the behaviour on a distinct **0-ary name** `n_p` (Terui §2.4 signature
  device; the nullary `∗` of Ex 2.14);
- **product** `A × B ↦` the `&`-behaviour — binary ramification `{1,2}` with projections
  `π₁,π₂` (Ex 2.14 `&&& = (x₁,x₂,{π₁(x₁),π₂(x₂)})`; internal completeness `N ∈ P&Q ⟺ N =
  π₁(x).P + π₂(x).Q`);
- **arrow** `A → B ↦` the (nonlinear) **function behaviour**, inhabited by Terui
  **function designs** (Def 2.17), the connective providing the name that opens the
  argument.

### 8.3 Forward map `⟦·⟧` (structural recursion on βη-long-normal terms)

- **variable** `x : τ` (η-long): the identity-free η-expansion forced by `τ` (Def 2.4
  η-long condition) — `⟦x⟧ = a(y⃗).(x|a⟨⟦y⃗⟧⟩)` unfolded to atomic leaves;
- **application** `h N₁⋯Nₙ` (head `h` a variable/constant): the positive action
  `h|a⟨⟦N₁⟧,…,⟦Nₙ⟧⟩`, `a` = the arrow-name of `h`'s type;
- **abstraction** `λx₁⋯xₙ.P`: the negative binder `a(x₁,…,xₙ).⟦P⟧`;
- **pair** `⟨M,N⟩`: `π₁(x).⟦M⟧ + π₂(x).⟦N⟧`; **projection** `πᵢ`: selection of the `πᵢ`
  branch.

`⟦M⟧` is **cut-free** (no `N₀|a⟨…⟩` with non-variable `N₀` — `M` β-normal), **identity-free**
(η-long ⇒ no bare-variable argument), **total** & **✠-free** (`M` a real term, no `Ω/✠`),
**deterministic** (every conjunction unary; the additive `+` of a pair is **deterministic
branching**, *not* the nondeterministic `⋀`). So `⟦M⟧` is a **proof design** (BT2010 Def
3.2), and its material part lies in `Gen!(B)`.

### 8.4 Read-back `⟪·⟫` (deterministic proof search, BT2010 §3.1)

By Def 2.4 every positive subterm of a cut-free identity-free design is `x|a⟨N₁,…,Nₙ⟩`
(args non-variable) `↦` application `(x) ⟪N₁⟫ ⋯ ⟪Nₙ⟫`; negative `a(x⃗).P ↦ λx⃗.⟪P⟫`;
`π`-branches `↦` pair/projection. **Single-valued**: BT2010 §3.1 (verbatim) — *"the head
variable `z` and the first positive action `a` completely determine the next positive rule
… bottom-up"* — so the design's structure forces a unique reading. The result is
βη-long-normal (cut-free ⇒ normal; identity-free ⇒ η-long).

### 8.5 Mutual inverse ⇒ bijection

By structural induction on the shared grammar (8.1):

- `⟪⟦M⟧⟫ = M` — constructor-by-constructor: abstraction↔binder, application↔positive
  action, pair/projection↔`π`-branches; α-names and loci are the only data forgotten, and
  `⟪·⟫` reintroduces them canonically (designs taken up to α-equivalence, Def 2.5).
- `⟦⟪D⟧⟧ = D` for `D ∈ Gen!(B)` — same induction; the **only** freedom is the immaterial
  negative components `Pb` (`b ∉ α0`), which **materiality** (`D = |D|_B`, Leg-3/Res-C)
  pins to `Ω`. On material designs the realizer is unique.

Hence `⟦·⟧` is a **bijection** `{βη-long-normal STLC {→,×,atom} terms} ⥲ Gen!(B)`, inverse
`⟪·⟫`. ∎

### 8.6 The two write-out watch-points, discharged

- **Product faithfulness.** `π₁ ≠ π₂` are distinct names; internal completeness (Ex 2.14)
  makes a pair design **exactly** `π₁(x).M + π₂(x).N`, and the two projections recover the
  two components — no two distinct pairs collapse, no quotient. The `+` is **additive
  (Opponent-)branching**, deterministic, *not* the nondeterministic `⋀` — so determinism
  (Def 3.2) is preserved.
- **η-long ↔ locative naming.** Terui Def 2.4's condition ("no argument a variable unless
  atomic") **is** η-long-normality; the name `a` at each node is **forced by the
  connective/type**, not chosen — so the locative presentation is determined, and the
  read-back has no naming freedom (beyond α, quotiented).

### 8.7 Nonlinear / contraction case (`g_r`)

A hypothesis used twice (the robustness `g_r = corr(t₁ m, t₁ (j q))`) is a **variable
heading two positive subterms** — a legal **non-linear** cut-free identity-free c-design
(Terui §1: the c-design syntax "encompasses … non-linear terms"; BT2010 §3.1: *"the
positive rule involves implicit uses of the contraction rule on positive behaviours"*).
The map handles it with no special case and **no `!`-box**; determinism is preserved
(reuse, not `⋀`). So `⟦g_r⟧ ∈ Gen!(B)`, as §5/§8.3 require.

### 8.8 Conclusion

**V-A′1 is proved** (paper proof, §8.5) — a representation bijection assembled from Terui
Def 2.4 + Ex 2.14 + Res-C, exactly as the final-gate cross-check (#2) anticipated and
**signed off**. With V-A′1 written out, the Res-A′ chain (Leg-1 native bijection ∘ Leg-2
design↔derivation by construction ∘ Leg-3 Res-C) is complete; together with the
already-cleared Res-C (D-C1), the D-A1 repair, and the D-A′1 re-grounding, **all residuals
of the T012 R-track are discharged.** ⟹ T012 → `established`; Q-032 → closed.

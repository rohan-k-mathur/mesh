# L4 — Aggregation: the ludics witness closes, and what it settles

**Date:** 2026-06-26
**Session:** [Session 20 — Diairesis cross-test](../10_IDEATION_SESSIONS/20-ludics-diairesis-crosstest.md), step **L4** (final — aggregation + report-back).
**Inputs:** [L0](L0-design-algebra-2026-06-26.md), [L1](L1-arrow-index-2026-06-26.md), [L2](L2-closure-monad-2026-06-25.md), [L3](L3-daimon-orientation-2026-06-26.md) — all CLOSED, all artifacts `--safe --without-K`, EXIT:0.
**Serves:** `removing-the-bound.md` (R2 / Precision Lemma) and `q013-approach.md` (§3 witness).
**Status:** Session 20 **COMPLETE**. One residual carried (M1′ `ν`-pole).

> **Aggregate verdict.** **Q-A (Precision Lemma at ludics): full CONFIRM** — designs are the initial algebra `μF` (L0), normalisation is recursion over ℕ (L1), behaviours are the closure monad of the orthogonality Galois connection (L2). **Q-B (arrow at ludics): CLOSES** — the arrow's two components are both imported: *irreversible progression* as the reduction order / ℕ-index (L1), and *orientation* as the posited daimon (L3); Risk 2 does not fire. Ludics — the candidate most likely to escape — falls wholly inside the paradigm and imports rather than generates the arrow. **One residual** (the M1′ `ν`-pole) is carried, not assumed.

---

## 1. The aggregate, object by object

| Object | (Co)algebraic structure | Q-A | Arrow component | Q-B | Step |
|---|---|---|---|---|---|
| **(D)** designs | initial algebra `μF`; `Design ≅ F Design` (Lambek) | CONFIRM | μ-pole carrier, imported | — | L0 |
| **(N)** normalisation | recursion over ℕ (step-index) | CONFIRM | **progression** = reduction order / ℕ | imported | L1 |
| **(O)** behaviours | closure monad of the orthogonality Galois connection | CONFIRM | — | — | L2 |
| **(O/N)** convergence | daimon-terminal required (`no-daimon-no-conv`) | — | **orientation** = posited daimon | imported | L3 |

**Why Q-B closes completely, not partially.** The arrow has two components — a *first/orientation* and *irreversible progression* (re-entry test; Q-016). The checks split exactly along them: L1 imported the progression (the reduction's "toward," the monotone ℕ-order), L3 imported the orientation (the posited `†` toward which interaction resolves). Together they exhaust the arrow's structure. There is no remaining component for the symmetric P/O interaction to have generated — which is precisely why Risk 2 cannot fire. The symmetry supplies the *duality* (Q-016: morphism-directedness, not the arrow); both arrow-components come from outside it.

## 2. Propagation to Q-013 — the dilemma is exhaustive across every serious candidate

The ludics witness was Q-013 §3's **last serious candidate**. With it cleared, the dilemma of generation (R3 / §2) now stands corroborated as exhaustive across three independent candidates, each falling into a horn by a different route:

| Candidate | Route | How it falls |
|---|---|---|
| Hegelian determinate negation | **Horn A** | `¬¬` is a closure operator — surplus climbs a pre-given order; the non-formalizable kernel *is* the arrow (negation-algebras pre-study) |
| Thermodynamic / physics arrow | **Horn B** | not lawlike directedness; requires the Past Hypothesis, a posited boundary condition (Roberts chs. 5–6; Callender's "bald posit") |
| Interactive ludics | **Horn B (definitional)** | the daimon is a posited terminal required for convergence; the symmetric P/O structure gives duality, not orientation (L3) |

**The cross-witness parallel that strengthens the case.** Physics and ludics produced the *same* steelman, independently: the Roberts wrinkle (electroweak T-violation as *primitive-lawlike* rather than *posited-boundary-condition*) and the Girardian reading (the daimon as *constitutive of interaction* rather than *posited from outside*). Both relocate the arrow's irreducibility from "posited" to "primitive-constitutive" — and **neither makes the arrow generated**. Two unrelated fields landing the same relocate-not-remove move is strong evidence that the posited-vs-primitive distinction is the only wiggle room available, and that *generation* is not on the menu in either.

**Q-013 verdict (the predicted FAIL, now at its supported grade): the arrow is irreducible** — not generated by any account examined, because structural generation imports it (Horn A) and processual / interactive generation presupposes it (Horn B), and the three strongest candidates each confirm this by a distinct mechanism. **Grade, stated honestly:** this is *FAIL conditional on the dilemma's exhaustiveness, with a clean sweep of the serious candidates* — not a theorem. Enumeration cannot close it (§4 of the approach), and the genuinely-tout-court (T2) version concerns the non-formalizable kernel, which is the arrow itself, reachable only by the transcendental argument (R3), not by any formal witness.

## 3. Propagation to the bound — the Precision Lemma at its strongest site

Ludics was the highest-value R2 test in `removing-the-bound.md` — the framework picked *because* it looked most like an escape (interactive, pre-logical, defined by testing not construction). It collapsed, object by object, into initial-algebra / recursion / closure-monad structure. So:

- **The Precision Lemma ("every precise account of self-differentiation is (co)algebraic") gains its strongest available confirmation.** Every prior candidate that collapsed (re-entry, `¬¬`-closure, Aczel AFA, Gödel-as-Lawvere) was already paradigm-adjacent; ludics was the one built to be otherwise, and it collapses too. This is the convergence-of-formalizations evidence that supports a Church–Turing-grade thesis, delivered at the hardest site.
- **The bound is therefore removable to Church–Turing grade for all precise accounts**, exactly as the scoping predicted. Not a theorem — the Lemma's antecedent is informal — but a thesis with one more, and the most demanding, formalization converging.

The realistic terminus that `removing-the-bound.md` §5 predicted is now reached: *proven for the paradigm; thesis-grade for all precise accounts (ludics the decisive confirmation); transcendentally argued for self-differentiation as such; theorem-grade tout-court proof impossible in principle.*

## 4. The single residual — the M1′ `ν`-pole

L0–L3 classify the **mechanized finite core** = the **μ-pole** (initial algebra, well-founded normalisation). The **infinitary** extension — Böhm-tree designs as the **final coalgebra `νF`**, and the coinductive normaliser — is the **`ν`-pole**, and it appears as the residual in *both* questions:

- **Q-A(D) ν-half:** infinitary designs = `νF` (the greatest fixed point).
- **Q-B ν-half:** the coinductive (non-terminating) normaliser.

**Predicted: imported the same way** — `νF` is the greatest-fixed-point *selection*, a pre-given co-recursion order, still imported (consistent with the coalgebra test's Check B/C and Q-015's bidirectional arrow, where both poles were imported). **Stated, not proven** — it needs coinduction / sized types, outside the `--safe --without-K` inductive corpus. This is the one place the ludics witness is *predicted-complete but not mechanized-complete*, and it is carried as such, not assumed away.

## 5. What is now settled, and at what grade

- **Q-B (arrow at ludics): CLOSED.** Imported, both components (L1 + L3). Modulo the `ν`-pole residual.
- **Q-A (Precision Lemma at ludics): full CONFIRM** for the finite core (D/N/O). Modulo the `ν`-pole residual.
- **Q-013 §3 witness: cleared.** R3's dilemma exhaustive across all three serious candidates.
- **Q-013 (arrow irreducible tout court): FAIL at thesis-grade** — dilemma + clean candidate sweep; in-principle not theorem-provable (the arrow = the non-formalizable kernel, T2).
- **The bound (removing-the-bound): removable to Church–Turing grade** for precise accounts; ludics the decisive confirmation.
- **Unchanged and standing:** distinction is *weakly prior* to the arrow (Q-016, FinSet witness, escapes I6); One is the bidirectional fold/unfold process (Q-015). The two-primitive base `{distinction, arrow}` is now: proven for the paradigm, thesis-grade tout-court for precise accounts, transcendentally argued for the non-formalizable, with distinction weakly prior.

**What remains genuinely open** (named, not buried): the `ν`-pole mechanization (M1′); a partial Lawvere-style universality theorem (R1) that would raise the Precision Lemma above pure convergence-evidence; and the R4 question of whether the bound is not merely removable-to-thesis-grade but *correct and final* (no neutral notion beyond precise accounts). None of these is on the critical path to the verdicts above; all are precisely scoped.

## 6. Registry lines

- **Q-013 §3:** ludics witness cleared — designs `μF` (L0), normalisation ℕ-recursion (L1), behaviours closure monad (L2), arrow imported via posited daimon + reduction order (L3). No interactive tertium.
- **Q-013:** FAIL (arrow irreducible) at thesis-grade — dilemma exhaustive across Hegel (Horn A), physics (Horn B), ludics (Horn B); steelman (primitive-vs-posited) relocates, does not generate.
- **Precision Lemma (R2):** CONFIRM at ludics, the strongest site; bound removable to Church–Turing grade for precise accounts.
- **Residual:** M1′ `ν`-pole (infinitary designs `νF` + coinductive normaliser), predicted-imported, stated not proven.
- **Standing:** distinction weakly prior (Q-016); One = fold/unfold (Q-015); two-primitive base at its predicted terminus grade.

## 7. Artifact index (the Isonomia-side deliverables)

All four type-check under `--safe --without-K`, Agda 2.7.0.1 / stdlib v2.0, no postulates/holes, EXIT:0:

| Step | Module | Establishes |
|---|---|---|
| L0 | [`ludics/DesignAlgebra.agda`](../mechanisation/agda/ludics/DesignAlgebra.agda) | `Design = μF` (initial algebra), `Design ≅ F Design` (Lambek), fold-initiality |
| L1 | [`ludics/ArrowIndex.agda`](../mechanisation/agda/ludics/ArrowIndex.agda) | normalisation = ℕ-recursion; verdict monotone/irreversible along `≤`; fuel gates progress |
| L2 | [`ludics/ClosureMonad.agda`](../mechanisation/agda/ludics/ClosureMonad.agda) | Galois connection `A ⊆ B^⊥ ⇔ B ⊆ A^⊥` + closure operator at the real `_⊥_` |
| L3 | [`ludics/DaimonOrientation.agda`](../mechanisation/agda/ludics/DaimonOrientation.agda) | `no-daimon-no-conv`; `dai-wins` (unilateral); `interact-not-symmetric` |

# AgoraBlueprint (archived 2024 → 2026)

This is a **stub**. The original 875-line `AgoraBlueprint.txt` has been
moved to `Internal_Documents/_archive/AgoraBlueprint.2024.txt` because
its file-aware predictions about how the algebra of arguments would be
implemented have all landed (Sprints A–E of the
*ECC Refinement and MCP Integration Plan*). Reading the blueprint as a
forward-looking document would now mislead.

## What landed where (one-line map)

| Blueprint section | Now lives in | Sprint |
|-------------------|--------------|--------|
| §0 "room as category" | `Claim` + `Argument` + `ArgumentSupport` (Prisma) | A |
| §1 atoms → algebra | `lib/argumentation/ecc.ts` typed `Arrow<A,B>` | A |
| Hom-set ∨ join | `join()` in `ecc.ts`; `ArgumentSupport` rows materialize Hom(I,B) | A |
| Internal hom [A,B] (warrant) | `Argument.aiProvenance` + `AssumptionUse(role: 'warrant')`; MCP `propose_warrant` writes them | E |
| Confidence monoids (min/product/DS) | Closed registry in `ecc.ts` (`MIN_MONOID`, `PRODUCT_MONOID`, `DS_MONOID`) — caller-supplied monoids forbidden by ECC plan §4 row 5 | A |
| Belief revision (culprits) | `culpritSets()` + cached `BeliefRevisionProposal` rows on grounded-OUT | A, D1 |
| Cross-room transport | `RoomFunctor` + `RoomTransportSnapshot` (one-hop only by contract — ECC plan §4 row 2) | C |
| Strict `isLogical` predicate | `isLogical()` in `ecc.ts` (ACCEPTED-only AND HUMAN-or-ratified — ECC plan §4 row 1) | A |
| AI-flagged warrants non-logical | `Argument.authorKind` + `derivationProvenance.humanRatified` | E |
| MCP / LLM agent surface | `packages/isonomia-mcp/src/server.ts` + `app/api/v3/deliberations/[id]/ecc/*` | E |

## Why archive instead of delete

The blueprint remains the cleanest narrative pitch of *why* the typed
ECC algebra is the right substrate for argument data. Keep it as
historical context; treat its file-paths and "future work" sections as
out-of-date.

## Authoritative replacements

* `Development and Ideation Documents/ARCHITECTURE/CATEGORICAL_FOUNDATIONS_ARCHITECTURE.md`
  — current architecture + grades (§13).
* `Development and Ideation Documents/ARCHITECTURE/ECC_REFINEMENT_AND_MCP_INTEGRATION_PLAN.md`
  — the plan whose Sprints A–E are now all landed.
* `packages/isonomia-mcp/README.md` — the MCP-facing tool catalog,
  including the "Categorical algebra" section (Sprint F).

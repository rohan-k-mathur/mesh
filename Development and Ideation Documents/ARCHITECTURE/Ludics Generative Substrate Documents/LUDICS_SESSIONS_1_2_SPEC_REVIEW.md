# LUDICS Sessions 1 & 2 — Spec Quality Review

**Review type:** spec ↔ substrate fidelity + architectural optimality (read-only).
**Question asked:** *"Is the spec the right spec?"* — not "is it implemented correctly" (that is the companion code-audit prompt).
**Specs under review:** [LUDICS_SESSION_1_DEV_SPEC.md](LUDICS_SESSION_1_DEV_SPEC.md) (1184 lines), [LUDICS_SESSION_2_DEV_SPEC.md](LUDICS_SESSION_2_DEV_SPEC.md) (985 lines).
**Substrate inputs read:** 10 Tier-1 documents, ~5,686 lines total.
**Methodology:** Two-pass review per spec. Pass A = substrate-fidelity rating (FAITHFUL / LOSSY / DISTORTED / OVER-COMMITTED / UNDER-COMMITTED). Pass B = architectural alternatives with 5-axis scoring on Correctness / Performance / Operability / Evolvability / Cognitive load.
**Reviewer notes:** [LUDICS_SESSIONS_1_2_SPEC_REVIEW_WORKING_NOTES.md](LUDICS_SESSIONS_1_2_SPEC_REVIEW_WORKING_NOTES.md) contains full per-finding evidence; this document is the distilled deliverable.

---

## §1 — Executive summary

**Top three fidelity findings (Pass A):**
1. **Phase 2e proof prompt anticipates the wrong outcomes (A12.1).** Session 2 §5's binary "confirm / counterexample-with-flag" branching missed the actual Outcome B (mis-attribution with structural refinement). Following §5.2 verbatim would have produced a misleading C1 update. **Highest-impact single finding.**
2. **`IncarnationSetManifest` singular `bottom` field (A9.1).** Reifies the obsolete unique-bottom-of-Inc(B) assumption Phase 2e disproved. Cascades into `claimedMinimalPremiseLociCount`, `ArticulationRecallScore.recall`, the mock client, all five corpus fixtures, and 22 tests.
3. **`compute_articulation_join` asserts a join always exists (A5.6).** Wrong post-2e for cross-cone inputs; OVER-COMMITTED at the API contract level. Drives the largest architectural finding too (B1, Δ +11).

**Top three architectural findings (Pass B):**
1. **B1 / B7 / B8 / B10 (cluster):** four rewrite-tier items, all tracing to the same root cause (T1 below). Mean Δ +11.
2. **B4 terminology drift (Δ +11).** `inference → daimon` rename + I1–I4 renumber. Lowest-cost high-value cleanup in either spec; should ship before any further consumer adoption locks in the wrong vocabulary.
3. **B9 fossilize-by-argument fuzzy lookup (Δ +12).** Spec ships a `canonicalText contains` interim path alongside the correct `argumentId` column. The interim path has nontrivial false-positive risk on production data; the dual-path approach should be replaced with column-first sequencing.

**One-sentence verdict per session:**
- **Session 1 spec is mostly faithful** with 4 DISTORTED findings (concentrated in §1 schema and §3 articulation API), 1 OVER-COMMITTED finding, and 7 UNDER-COMMITTED findings (concentrated in test/invariant discipline) — 2 architectural commitments worth rewriting, 4 worth augmenting.
- **Session 2 spec carries more substrate-fidelity defects** with 9 DISTORTED findings, 1 OVER-COMMITTED, 12 UNDER-COMMITTED, concentrated in §2 (`incarnationSet`), §3 (`proposeSynthesis`), and §5 (OQ-JSL proof prompt) — 4 architectural commitments worth rewriting, 2 worth augmenting.

---

## §2 — Pass A findings table

Rating shorthand: **F** = FAITHFUL · **L** = LOSSY · **D** = DISTORTED · **OC** = OVER-COMMITTED · **UC** = UNDER-COMMITTED.

### Session 1

| # | Phase | Contract | Substrate artefact | Rating | Evidence / Note |
|---|---|---|---|---|---|
| A1.1 | 1a §1 | `LudicMove.moveType` enum has `inference` | Substrate `daimon` (✝) | D | Terminology drift; every audit must translate |
| A1.2 | 1a §1 | `Design.loci String[]` (Postgres text array) | Set of loci ⊆ universe | F | Set semantics implicit (de-dup at caller) |
| A1.3 | 1a §1 | `DesignInclusion` as edge table | ≤_⊆ partial order on B | F | Edge materialization is operationally sound |
| A1.4 | 1a §1 | `WitnessRecord.stratumLabel` mirror of move | Witnessing layer flag | F | Allows witness-side filters |
| A2.1 | 1b §2.1 | Invariants I1–I4 | Substrate I1–I4 (different content) | D | Naming collision; audits ambiguous |
| A2.2 | 1b §2.2 | Tier-2 invariants T1–T5 | Substrate triad 𝒯_L = (P_L, N_L, ⊥_L) | UC | Spec T1–T5 are operational; substrate T1–T5 are formal — separate concepts share labels |
| A2.3 | 1b §2 | "Invariant violations raise" | Substrate: invariants are intrinsic | F | Defensive runtime checks acceptable |
| A3.1 | 1c §3.1 | `exposureMap(deliberationId)` returns positions | σ(𝒟_P)^⊥ enumeration | F | Result type matches Reading C |
| A3.2 | 1c §3.1 | `stratumLabel` as stored column | Derived from current 𝒟_P | D | Will go stale on neighbour writes (echoes T3) |
| A3.3 | 1c §3.1 | `walked` / `witnessable` / `latent` trichotomy | Substrate stratum has same three | F | — |
| A4.1 | 1d §3.2 | `articulationLattice(deliberationId)` returns lattice | (B, ≤_⊆) | L | Returns lattice flag, not Cone structure |
| A4.2 | 1d §3.2 | `bottom` field returned with B | Inc(B) is antichain post-2e | D | Singular `bottom` not defined post-2e |
| A5.1 | 1e §3.2 | `DesignInclusion` treated as lattice spine | (B, ≤_⊆) decomposes into cones | D | Spec assumes connected order; cones are disjoint |
| A5.6 | 1e §3.2 | `compute_articulation_join(designIds[])` asserts join | ∨_⊥⊥ exists same-cone only | **OC** | API will throw or return wrong shape for cross-cone inputs |
| A5.7 | 1e §3.2 | `delocationType: "locus-addition-required"` | Daimon Lock Lemma cone-extension | UC | Single shape conflates legitimate delocation with cross-cone non-join |
| A6.1 | 1f §1 | `retractReason` flat enum (4 values) | T4 separates dialectical-side / witness-side | L | Layer collapse baked into schema |
| A6.2 | 1f §3.3 | `walkDesign()` / `extendDesign()` tools | Substrate move + extension discipline | F | — |
| A7.1 | 1g §5.3 | Briefing fingerprint = single SHA256 over 15 fields | Tier-2 N-C24 freshness discipline | L | Opaque to which of R1–R5 fired |
| A7.2 | 1g §5.3 | R1–R5 material-change rules listed | Substrate R1–R5 | F | Rules are correctly stated |
| A7.3 | 1g §6 | MCP auth: shared bearer + caller participantId | T5 consumer surface | UC | No cross-participant abuse mitigation |

**Session 1 severity tally:** F: 9 · L: 3 · D: 4 · OC: 1 · UC: 4.

### Session 2

| # | Phase | Contract | Substrate artefact | Rating | Evidence / Note |
|---|---|---|---|---|---|
| A8.1 | 2a §1.2 | `prisma migrate dev` | n/a (operational) | F substrate / **repo-convention violation** | AGENTS.md mandates `prisma db push` (T7) |
| A8.2 | 2a §1.2 | Seed script: `DesignInclusion` between two designs | Cones disjoint (Phase 2e) | UC | Silent on cone-locality |
| A8.3 | 2a §1.3 | p95 perf targets (500/200/100 ms) | n/a | F | Defensible operational |
| A8.4 | 2a §1.3 | Test 11: fingerprint stability only | R1–R5 invalidation rules | UC | 0/5 invalidation rules tested — ✅ closed 2026-05-21 (see §9) |
| A8.5 | 2a §1.3 | Seed `stratumLabel = "walked"` static | Derived state from σ(𝒟_P)^⊥ | UC | No test for neighbour relabeling |
| A9.1 | 2b §2.2 | `IncarnationSetManifest.bottom: DesignSummary \| null` | Inc(B) is antichain (Phase 2e) | **D** | Cascades into 4 sub-fields, 22 tests |
| A9.2 | 2b §2.6 | "co-equal hub topology" framed as exceptional | Antichain is generic post-2e | D | Fixture distribution reflects pre-2e priors |
| A9.3 | 2b §2.4 | `recall = claimedMinimalLociCount / bottomLociCount` | Per-cone, no global bottom | D | Single-number recall breaks |
| A9.4 | 2b §2.4 | `ConfidentMisstatementKind: "incarnation-undercount"` | Multiple incarnations generic | UC | No `miscount-across-cones` or `incarnation-conflation` kinds |
| A9.5 | 2b §2.5 | Mock-client perfect-recall path depends on `bottom !== null` | n/a | D | Same singular-bottom defect |
| A10.1 | 2c §3.3 | `proposeSynthesis` returns `delocationType` on `newLoci > 0` | ∨_⊥⊥ undefined cross-cone | **D + OC** | Conflates legitimate delocation with cross-cone non-join |
| A10.2 | 2c §3.5 | Fixture: `bottom.loci.length = sum of inputs` | Union ≤ sum; bottom ⊆ join | D | Fixture arithmetic wrong post-2e |
| A10.3 | 2c §3.2 | `SyntheticReadout.bottomIncarnation` field | Per-cone, no global | D | Same defect (A4.2 / A9.1) |
| A10.4 | 2c §3.6 | Log "slow joins (`closureSteps > 5`)" | Within-cone ∨ = literal ∪; closureSteps always 0 | D | Any nonzero = substrate violation |
| A10.5 | 2c §3.3 | `schemeKey: "synthesis"` | Not in substrate scheme catalogue | UC | No CQs, validity conditions specified |
| A10.6 | 2c §3.6 | `proposeSynthesis` idempotence on `biorthoClass` | Substrate I2 | F | ✅ |
| A11.1 | 2d §4.4 | `argumentId String?` on `LudicMove` | Back-pointer lives on witness side | L | Direction reversed; small T4 leak |
| A11.2 | 2d §4.3 | Fuzzy `canonicalText contains` interim path | n/a | UC | False-positive risk; ships alongside correct `argumentId` path |
| A11.3 | 2d §4.5 | Manual retract endpoint | A1–A4 announcement discipline | UC | Silent on notification integration |
| A11.4 | 2d §4.6 | `fossilize()` idempotent / endpoint returns 409 | n/a | **Intra-spec incoherence** | Split behaviour at function vs. endpoint layer |
| A11.5 | 2d §4 | `retractReason` lifecycle wiring | T4 separation | L | Confirms A6.1; makes the collapse concrete |
| A12.1 | 2e §5.2 | Branching: confirm / counterexample-with-`joinIsMinimal`-flag | Actual: Outcome B mis-attribution + structural refinement | **UC** | Highest-regret finding; spec stale on arrival |
| A12.2 | 2e §5.2 | Branching only allows C1 "upgrade" | C1 was downgraded to Corrected | UC | No "downgrade" path |
| A12.3 | 2e §5.3 | References: Girard, Melliès | Actual proof uses Fouqueré–Quatrini 2013 | UC | Key citation missing |
| A13.1 | 2f §6.1 | Rate-limit by `participantId` | n/a | F / UC | Cross-participant abuse vector unaddressed |
| A13.2 | 2f §6.2 | BullMQ cache warming | n/a | F | — |
| A13.3 | 2f §6.3 | "`closureSteps > 5` = unusual position complexity" | Substrate violation post-2f | D | Interpretation stale |
| A13.4 | 2f §6.4 | Extract auth to `server/ludics/auth.ts` | n/a | F | — |
| A13.5 | 2f §6.5 | L1 in-memory + L2 DB cache | n/a | F / UC | Cross-instance L1 coherence unspecified |
| A13.6 | 2f §6.6 | Test wording: "upsert: `ruleLabel` updated to 'R2'" | Hash changes ⇒ new row, not update | D | Test contradicts schema intent |

**Session 2 severity tally:** F: 6 · L: 2 · D: 9 · OC: 1 · UC: 12.

---

## §3 — Pass B findings table

| # | Phase | Commitment | Spec choice | Alternatives | Winner | Spec wins? | Recommendation |
|---|---|---|---|---|---|---|---|
| B1 | S1 §3.2 | Articulation join algorithm | Eager closure, asserts join exists | (2) Discriminated result (3) Two-call API | Alt-2 (23/25) | No (12/25) | **REWRITE** — discriminated result `{ kind: "same-cone-join" \| "cross-cone-no-join" \| "same-cone-no-extension" }` ✅ shipped 2026-05-21 (ratified as `same-cone-join` \| `same-cone-delocation-required` \| `cross-cone-rejected` per [LUDICS_SESSION_1_DEV_SPEC.md](./LUDICS_SESSION_1_DEV_SPEC.md) §3.2 — see §9) |
| B2 | S1 §1 | `stratumLabel` storage | Stored column | (2) Derived view (3) Materialized view (4) Verify-on-read cache | Alt-4 (21/25) | No (16/25) | **AUGMENT** — stored cache + cheap version check + recompute on mismatch |
| B3 | S1 §1 | `retractReason` enum | Single flat enum (4 values) | (2) Tagged `retractLayer + retractReason: string` (3) Separate `RetractionEvent` table | Alt-2 (22/25) | No (17/25) | **AUGMENT** — split into tagged columns, preserve T4 |
| B4 | S1 §1, §2.1 | Terminology (`inference`, I1–I4) | Keep, document mapping | (2) Rename to substrate terms (3) DB alias | Alt-2 (25/25) | No (14/25) | **REWRITE** (mechanical) — `inference → daimon`, I1–I4 → S1–S4 ✅ shipped 2026-05-21 (Prisma + write seam already correct; 4 doc-comment stragglers patched — see §9) |
| B5 | S1 §5.3 | Briefing fingerprint hash | Single SHA256 over 15 fields | (2) Component hash vector (3) Merkle tree | Alt-2 (23/25) | No (19/25) | **AUGMENT** — `{ hubHash, refusalHash, claimGraphHash, ... }` ✅ shipped 2026-05-21 (additive — see §9) |
| B6 | S1 §6 | MCP auth | Shared bearer + caller participantId | (2) Per-tenant JWT (3) mTLS (4) Scoped session tokens | Alt-4 (22/25) | No (19/25) | **FINE v1 / AUGMENT v2** — scoped session tokens bound to `(deliberationId, participantId)` |
| B7 | S2 §2.2 | `IncarnationSetManifest` shape | `{ bottom, minimals[], totalIncarnations }` | (2) `{ incarnations[], totalIncarnations }` (3) Per-cone `{ cones[] }` | Alt-2 (24/25) | No (12/25) | **REWRITE** — drop `bottom`; consider Alt-3 once MCP exposes cone identity ✅ shipped 2026-05-21 (Alt-2 with optional `cones?` field for Alt-3 forward-compat — see §9) |
| B8 | S2 §3.3 | `proposeSynthesis` failure model | Assumes joinable | (2) Discriminated kinds (3) Two-step validate→synthesize | Alt-2 (23/25) | No (14/25) | **REWRITE** — mirror B1's discriminated pattern ✅ shipped 2026-05-21 (`SynthesisProposalResult` is now a discriminated union by `kind` — see §9) |
| B9 | S2 §4.3 | `fossilizeByArgument` lookup | Dual path (fuzzy + `argumentId`) | (2) `argumentId`-only (3) Event-bus | Alt-2 (25/25) | No (13/25) | **REWRITE** — column-first; drop fuzzy interim ✅ shipped 2026-05-21 (variant — see §9) |
| B10 | S2 §5.2 | OQ-JSL proof outcome model | Binary confirm / counterexample-with-flag | (2) Three-bucket (confirm / refute-local / refute-structural) (3) Exploratory + formal | Alt-2 (24/25) | No (12/25) | **REWRITE (template)** — permanent template for formal-proof spec sections |
| B11 | S2 §6.1 | Rate-limit key | `participantId` | (2) Compound `(tenant, participant) + (tenant, IP)` (3) Per-IP (4) Token-bucket | Alt-2 (20/25) | Tie (19/25) | **FINE v1 / AUGMENT v2** — compound key for cross-participant abuse |
| B12 | S2 §6.5 | Fingerprint cache coherence | L1 in-memory + L2 DB | (2) Drop L1; Redis read-through (3) L1+L2+pub/sub | Alt-2 (24/25) | No (16/25) | **AUGMENT** — drop L1; single shared Redis cache |

**Pass B roll-up:**
- **Rewrite-tier (6):** B1, B4, B7, B8, B9, B10 (Δ +9 to +12).
- **Augment-tier (5):** B2, B3, B5, B12, + B11/B6 deferred to v2.
- **Spec choice scored lower than the best alternative on all 12 commitments.** Performance was a strong design driver (spec scored 4–5 on Performance for 10/12); Correctness was the weak axis (spec scored 1–3 on Correctness for the six rewrite-tier items).

---

## §4 — Cross-cutting findings

### §4.1 — Singular-bottom assumption (root cause behind 13 findings)
Both specs assume Inc(B) has a unique bottom |B|. Phase 2e (proof landed 2026-05-21) disproved this; Inc(B) is an antichain, (B, ≤_⊆) decomposes into disjoint cones, and ∨_⊥⊥ is well-defined only within a cone (literal chronicle-set union under Phase 2f Reading A). Both specs were written 2026-05-19/20, 1–2 days before the proof. **Not a quality failure — a sequencing artifact.** But it concentrates regret in §2, §3, §5 of Session 2 and §3.2 of Session 1.

Findings tracing to this root cause: A4.2, A5.1, A5.6, A9.1, A9.2, A9.3, A9.5, A10.1, A10.3, A10.4, A12.1, B1, B7, B8, B10. **15 items total.**

### §4.2 — Terminology drift from substrate
Five separate drifts (A1.1, A2.1, A9.2, A10.4/A13.3, A10.5). Spec invariant numbering collides with substrate; move-type vocabulary diverges; closure-step interpretation reverses from "correctness gauge" to "performance gauge." **One coordinated terminology pass eliminates all five.**

### §4.3 — Derived state treated as stored state
Four cases (A3.2, A9.1, A7.1/B5, A13.5/B12). Substrate is replete with derived state; spec defaults to stored columns with manual sync, generating staleness risk surfaces. Common remedies (verify-on-read, component vectors, single-shared-cache) are well-known but not adopted.

### §4.4 — T4 boundary leakage
Three cases (A11.1 reversed back-reference; A6.1/A11.5/B3 enum collapse; A11.3 unowned announcement discipline). The substrate's clean separation between the dialectical layer and the witnessing layer is partially collapsed at the Prisma schema boundary.

### §4.5 — Operational concerns added without substrate hooks
Rate limiting, cache warming, observability, auth, durable history — all FAITHFUL on Pass A but under-specified failure modes (cross-participant abuse, concurrent rule firing, L1/L2 coherence, `closureSteps > 0` policy).

### §4.6 — Test discipline gaps
- R1–R5 fingerprint invalidation rules: **0/5 exercised against real data** through end of Session 2 (A8.4).
- Stratum-recompute never tested (A8.5).
- Function-layer idempotence ≠ endpoint-layer idempotence (A11.4 — intra-spec incoherence).
- Adversarial fixture arithmetic mathematically inconsistent post-2e (A10.2).

### §4.7 — Repo-convention violation
A8.1 — `npx prisma migrate dev` against the directive in `.github/copilot-instructions.md` and `AGENTS.md` to use `prisma db push`.

---

## §5 — Regret-check inventory

*Specs that should be rewritten now that OQs they predated have closed.* This is the highest-value section of the review.

### OQs closed since spec was written
| OQ | Resolution | Date | Spec sections affected |
|---|---|---|---|
| OQ-JSL (is `(Inc(B), ≤_⊆, ∨_⊥⊥)` a JSL?) | **Outcome B mis-attribution.** Inc(B) is an antichain; JSL carrier is per-cone C_i. | 2026-05-21 | S1 §3.2 · S2 §2 · S2 §3 · S2 §5 · S2 §6.3 |
| ∨_⊥⊥ within-cone semantics | **Literal chronicle-set union** (Reading A, Phase 2f pre-session) | 2026-05-21 | S1 §3.2 · S2 §3.5 |
| Cone Decomposition Proposition | (B, ≤_⊆) = disjoint union of cones C_i; ∨ exists same-cone only | 2026-05-21 | S1 §3.2 · S2 §2.6 · S2 §3.3 |
| Daimon Lock Lemma | Same-cone designs share positive (daimon) skeleton; differ only in negative branches | 2026-05-21 | S2 §1 seed script construction |

### Stale-on-arrival sections (cannot be augmented; require rewrite)
| Section | Why stale | Tier |
|---|---|---|
| S1 §3.2 `compute_articulation_join` | Assumes unique bottom; asserts join exists | **REWRITE** ✅ shipped 2026-05-21 (see §9 B1) |
| S2 §2 `incarnationSet` manifest + recall metric | Singular `bottom` field; single-number recall | **REWRITE** ✅ shipped 2026-05-21 (see §9 B7) |
| S2 §3 `proposeSynthesis` | Assumes joinable; conflates delocation with cross-cone failure; fixture arithmetic wrong | **REWRITE** ✅ shipped 2026-05-21 (see §9 B8) |
| S2 §5 OQ-JSL proof prompt | Proof has already executed with Outcome B; spec branching never anticipated structural refinement | **ARCHIVE** with pointer to [LUDICS_OQ_JSL_PROOF.md](LUDICS_OQ_JSL_PROOF.md) |
| S2 §6.3 `closureSteps` observability paragraph | "Slow join" interpretation wrong; any nonzero is substrate violation | **REWRITE** (paragraph) ✅ shipped 2026-05-22 (see §9 S2 §6.3) |
| S2 §1.2 seed script | Silent on cone-locality of `DesignInclusion` edge | **REWRITE** (script) ✅ verified 2026-05-22 (see §9 S2 §1.2) |

**Counts:** 6 REWRITE + 1 ARCHIVE.

### Risk-weighted dependency order for downstream specs
Session 3 spec authors should treat the inventory as a dependency graph, not a flat list.

1. **Tier-1 blockers (do first; everything downstream depends on these):**
   - **B4 terminology pass** — mechanical; unblocks every audit and consumer doc.
   - **REWRITE S2 §2** — every Session 2+ feature touches `IncarnationSetManifest`.
   - **REWRITE S1 §3.2** — every synthesis path calls `compute_articulation_join`.
   - **ARCHIVE S2 §5** — formally close the loop with a pointer to actual outcome.

2. **Tier-2 high-priority (before next AI-consumer-facing feature):**
   - **REWRITE S2 §3 `proposeSynthesis`** — depends on Tier-1 (B7, B1 rewrites).
   - **AUGMENT S2 §4 (B9)** — drop fuzzy fossilize path (production correctness risk).
   - **AUGMENT S2 §6.5 (B12)** — drop L1; Redis-only (production correctness risk).

3. **Tier-3 hygiene (do when touching the area):** B5, B2, B3, B11, T7 migration tooling.

4. **Tier-4 future-proofing (defer to next major auth/observability cycle):** B6 scoped session tokens.

---

## §6 — Triage (three bins)

### Bin 1 — Spec is wrong, rewrite (7 items)
*Highest priority. Fix before any new code is written against the affected sections.*

| Section | Driver |
|---|---|
| S1 §3.2 articulation join algorithm | A5.6, B1 |
| S2 §1.2 seed script | A8.2 |
| S2 §2 `IncarnationSetManifest` | A9.1, B7 |
| S2 §3 `proposeSynthesis` | A10.1, A10.2, A10.3, B8 |
| S2 §5 OQ-JSL proof prompt (**ARCHIVE**) | A12.1, B10 |
| S2 §6.3 `closureSteps` paragraph | A10.4, A13.3 |
| S1 §1 + §2.1 terminology pass (B4) | A1.1, A2.1 |

### Bin 2 — Spec is right but under-specified, augment (12 items)
*Add the missing invariant, failure mode, or enforcement point.*

| Section | Driver |
|---|---|
| S1 §1 `LudicMove.stratumLabel` column | B2 — verify-on-read |
| S1 §1 `WitnessRecord.retractReason` | B3 — tagged columns |
| S1 §5.3 briefing fingerprint hash | B5 — component vector ✅ shipped 2026-05-21 |
| S2 §1.1 migration command | T7 — `prisma db push` |
| S2 §1.3 integration tests | A8.4 (R1–R5 coverage), A8.5 (stratum) |
| S2 §4.3 `fossilizeByArgument` | B9 — drop fuzzy path ✅ shipped 2026-05-21 |
| S2 §4 fossil endpoint idempotence | A11.4 ✅ shipped 2026-05-21 |
| S2 §4.5 manual retract endpoint | A11.3 — announcement integration policy |
| S2 §6.1 rate-limit | B11 — compound key (v2) |
| S2 §6.5 `BriefingFingerprintHistory` | B12 — drop L1; Redis-only |
| S1 §6 / S2 §6.4 MCP auth | B6 — scoped session tokens (v2) |
| S2 §2.4 `ConfidentMisstatementKind` | A9.4 — add `miscount-across-cones`, `incarnation-conflation` |

### Bin 3 — Spec is fine, kept for the record (5 items)
*No action; documented for future reviewers.*

| Section | Note |
|---|---|
| S1 §1 Prisma core shapes (Behaviour, Design, DesignInclusion, WitnessRecord) | Modulo enum/tag cleanups above |
| S1 §6 test harness structure | Sound |
| S2 §1 performance SLAs (500/200/100 ms p95) | Defensible operational targets |
| S2 §6.2 BullMQ cache warming | Orthogonal to substrate; correctly scoped |
| S2 §6.4 auth extraction to `server/ludics/auth.ts` | Good factoring |

**Triage totals:** 7 REWRITE/ARCHIVE · 12 AUGMENT · 5 FINE.

---

## §7 — Outstanding questions

Surfaced by the review; could not be resolved from spec + substrate reading alone.

1. **OQ-C3-thin-cones** (flagged in Phase 2f pre-session notes). What is the formal characterization of "thin cones" — cones with |C_i| bounded by a small constant? Affects whether per-cone join can be sub-linear in |B|.
2. **Cone identity in the MCP surface.** Does the AI consumer need explicit `coneId` exposure, or is the `incarnations[]` array sufficient? Decides whether B7 Alt-2 (drop `bottom`) or B7 Alt-3 (per-cone manifest) is the right rewrite.
3. **Cross-cone synthesis semantics.** When two designs occupy different cones, is there a *Mesh-layer* (non-substrate) "discourse synthesis" operation that fills the gap that ∨_⊥⊥ no longer covers? Spec currently silent.
4. **Announcement discipline ownership.** A1–A4 deferred at substrate, silent in spec. Manual retract endpoint (A11.3) needs a home for "who notifies whom" — substrate, spec, or a new layer.
5. **Concurrent rule firing for `BriefingFingerprintHistory.ruleLabel`.** When R1 and R2 fire on the same write, which is recorded? Affects auditability of cache-invalidation events.
6. **`closureSteps > 0` policy.** ~~Hard error / soft alert / drop-result?~~ **CLOSED 2026-05-22 → hard error.** Ratified post-B8: `synthesisProposalAgent.ts` throws `SynthesisError("CLOSURE_STEPS_INVARIANT", …, 422)` on nonzero same-cone `closureSteps`; `articulationLattice.ts` retains the WARN log for observability. See [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §6.3 policy box.
7. **Terminology rename cost.** B4 recommends `inference → daimon` and I1–I4 → S1–S4. The DB migration is mechanical; the API/MCP-tool-name impact on existing AI consumers is the open question — has any external consumer hard-coded `moveType: "inference"`?

---

## §8 — Closing observations

**Meta-finding.** The substrate-spec gap is **healable in one focused sprint**. Four of six rewrite-tier items share a single root cause (T1). Fixing the singular-bottom assumption and doing the B4 terminology pass would resolve **11 of the 15 cross-cutting findings**.

**Pattern of strength.** Both specs scored 4–5 on Performance for nearly every commitment. Operational sections (BullMQ cache warming, perf SLAs, auth extraction) were uniformly FAITHFUL. The spec's structural weakness is **substrate fidelity**, not engineering judgment.

**Pattern of weakness.** The recurring failure mode is **adopting the staleness-prone storage option** (stored column over derived view; single hash over component vector; L1+L2 over single shared cache; flat enum over tagged columns). A future Session 3 spec template could include a "derived state checklist" prompting the author to argue against the staleness-prone default.

**Sequencing lesson.** Both specs were written 1–2 days before the OQ they implicitly depended on closed. A spec lifecycle convention — "do not commit a spec section whose primary OQ is open at the time of writing" — would have prevented the largest finding cluster. This is a process recommendation, not a quality criticism of the spec authors.

---

## §9 — Resolution log (2026-05-21)

Six items from this review have shipped (four Bin-2 + two Bin-1). Annotated with ✅ in §2 (Pass A), §3 (Pass B), and §6 (triage) above.

| Item | Bin | Resolution | Artefacts |
|---|---|---|---|
| **A11.4** — `fossilize()` function-vs-endpoint idempotence | Bin 2 | DONE | Direct unit test pinning function-layer idempotent return + endpoint-layer 409. Splits the behaviour at the correct layer per the spec's intent. |
| **B9** — drop fuzzy `fossilizeByArgument` lookup | Bin 2 | DONE (variant) | The fuzzy `canonicalText contains` path was already removed in earlier `P1.h.5` hardening. This round closed the adjacent gap surfaced by the audit: the seed script never wrote `argumentId` on `LudicMove` rows. [scripts/seed-ludics-showcase.ts](../../../../scripts/seed-ludics-showcase.ts) (L85–L116) now sets `argumentId`; the 143 NULL rows on staging were backfilled idempotently. New T15.2 in [__tests__/invariants/phase2d-fossil-lifecycle.test.ts](../../../../__tests__/invariants/phase2d-fossil-lifecycle.test.ts) pins the structural-where contract for `fossilizeByArgument` (asserts `Object.keys(where).sort() === ["fossilizedAt", "ludicMove"]` — no `canonicalText`, no `OR`). |
| **B5** — briefing fingerprint component vector | Bin 2 | DONE (additive) | `ComponentHashVector { hubs, frontier, refusal, witnessing }` added to `BriefingFingerprintResult` in [server/ludics/briefingFingerprint.ts](../../../../server/ludics/briefingFingerprint.ts). Disjoint partition over `MaterialFields` (refusalCount deliberately omitted — derivable from `refusalConclusionIds`). `evaluateStalenessRules` takes optional `(prevComponents, currComponents)` and skips rules whose owning component is unchanged: R1↔hubs · R2↔refusal · R3/R4↔frontier · R5↔witnessing. New `diffComponents(prev, curr)` helper. New pin file [__tests__/invariants/phase1f-fingerprint-components.test.ts](../../../../__tests__/invariants/phase1f-fingerprint-components.test.ts). |
| **A8.4** — R1–R5 invalidation coverage | Bin 2 | DONE | New end-to-end test file [__tests__/invariants/phase1f-r1r5-coverage.test.ts](../../../../__tests__/invariants/phase1f-r1r5-coverage.test.ts) — 7 tests, one per R1/R2/R3/R4/R5 + no-change control + priority-order (R1 wins over R5). Each rule test mocks `computeSyntheticReadout`/`derivePrioritizedOpenCqs`/`getDeliberationSchema` twice, calls `computeBriefingFingerprint` twice, asserts `lastMaterialChangeRule === "Rx"`, asserts `diffComponents(first.components, second.components)` returns exactly the expected single component (disjointness pin), then on a third mock asserts `checkBriefingFingerprint(…, first.contentHash)` returns `{ stale: "Rx" }`. Per-test deliberation IDs (`delib-r1r5-Rx-N`) isolate the module-level cache. Closes Pass A §4.6 0/5-coverage gap. |
| **B4** — terminology pass (`inference → daimon`, I1–I4 → S1–S4) | Bin 1 | DONE | Discovery: most of B4 had already shipped silently. Prisma schema at [lib/models/schema.prisma](../../../../lib/models/schema.prisma) line 10258 already uses `moveType String   // "positive" \| "negative" \| "daimon"`; [server/ludics/bindParticipantToDesign.ts](../../../../server/ludics/bindParticipantToDesign.ts) already returns `invariantChecks: {S1_existingLocus, S2_existingStructure, S3_canonPipelineGated, S4_schemeTyped}`; MCP response shape already uses S1–S4. This round patched the four stale-label doc-comment sites: `bindParticipantToDesign.ts` doc comment (`"inference move" → "daimon move"`); [app/api/v3/ludics/bind-witness/route.ts](../../../../app/api/v3/ludics/bind-witness/route.ts) header block (I1–I4 → S1–S4 + daimon wording); [__tests__/invariants/phase1b-bind-seam.test.ts](../../../../__tests__/invariants/phase1b-bind-seam.test.ts) header + section comments (I1/I2/I3/I4 → S1/S2/S3/S4 + `it("skips the S4 catalog lookup…")`); [packages/isonomia-mcp/src/server.ts](../../../../packages/isonomia-mcp/src/server.ts) ~L1751 tool description (`(I1)/(I2)/(I3)` → `(S1)/(S2)/(S3)` and error-code mapping `(I1/I2 fail)/(I3 fail)/(I4 fail)` → `(S1/S2 fail)/(S3 fail)/(S4 fail)`). AIF-domain `targetScope: "inference"` in `experiments/polarization-1*` is unrelated (AIF premise/inference/conclusion attack scope, not Ludics polarity) and intentionally untouched. |
| **S2 §5 archive** — OQ-JSL proof prompt | Bin 1 | DONE | [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §5 has been rewritten as a 3-paragraph pointer block ("[ARCHIVED post-review]") summarising Outcome B — mis-attribution with structural refinement (Inc(B) is an antichain; ∨_⊥⊥ is per-cone; (B, ≤_⊆) decomposes into disjoint cones), with links to authoritative records [LUDICS_OQ_JSL_PROOF.md](./LUDICS_OQ_JSL_PROOF.md) + [LUDICS_ORDER_RELATION_DEFINITION.md](./LUDICS_ORDER_RELATION_DEFINITION.md). Verbatim provenance archive at [LUDICS_SESSION_2_DEV_SPEC.archived.md](./LUDICS_SESSION_2_DEV_SPEC.archived.md) ("Archived §5 — Phase 2e…"). Downstream-sections-rewritten list and lessons-captured paragraph included in the replacement. |
| **B7** — `IncarnationSetManifest` antichain rewrite | Bin 1 | DONE (Round 6, 2026-05-21) | Singular `bottom: DesignSummary \| null` and `minimals: DesignSummary[]` fields dropped from `IncarnationSetManifest` in [eval/ai-epi/types.ts](../../../../eval/ai-epi/types.ts) and replaced with antichain `incarnations: DesignSummary[]` + optional `cones?: Array<{coneId, bottomIncarnation}>` for Alt-3 forward-compat (MCP cone-identity OQ from §7). Per-cone semantics cascade fully landed: `BriefingClaim` swaps scalar `claimedMinimalPremiseLociCount` for `claimedIncarnationCount: number` + `claimedMinimalPremiseLociCountPerCone: number[]`; `ArticulationRecallScore` swaps scalar `recall` for `{ perCone[], meanRecall, incarnationCountRecall, miscountAcrossCones }`; `ConfidentMisstatementKind` gains `"miscount-across-cones"` (resolving the A9.4 first item). Logic ports in [eval/ai-epi/manifestGenerator.ts](../../../../eval/ai-epi/manifestGenerator.ts) (empty default), [eval/ai-epi/llm/mockClient.ts](../../../../eval/ai-epi/llm/mockClient.ts) (per-cone emission), [eval/ai-epi/scorecard/phase1.ts](../../../../eval/ai-epi/scorecard/phase1.ts) (per-cone misstatement loop + per-cone meanRecall). All 7 v2 fixtures migrated per spec [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §2.6 table (large-real → 8 cones `[12,9,7,6,6,5,5,4]`; small-diffuse → 3 cones `[4,3,3]`; small-coequal → 2; refusal-rich / single-hub / synthesis-{join,delocation} → 1 each); `readout.contentHash` was not the migrated surface so manifest pins still validate. [__tests__/invariants/phase2b-incarnation-manifest.test.ts](../../../../__tests__/invariants/phase2b-incarnation-manifest.test.ts) fully rewritten (22 → 23 tests; added `T16b` single-cone `miscount-across-cones` guard); [__tests__/invariants/phase2c-synthesis-workflow.test.ts](../../../../__tests__/invariants/phase2c-synthesis-workflow.test.ts) T14/T15/T17 updated. Aggregate: 18 suites · 344 tests green. |
| **B1** — `compute_articulation_join` discriminated result | Bin 1 | DONE (verified Round 6, 2026-05-21) | Verification round, not a code rewrite: the Pass B proposed kinds (`cross-cone-no-join`, `same-cone-no-extension`) were the audit's draft names; the ratified canonical names per [LUDICS_SESSION_1_DEV_SPEC.md](./LUDICS_SESSION_1_DEV_SPEC.md) §3.2 are the more descriptive `same-cone-join` / `same-cone-delocation-required` / `cross-cone-rejected` and they were already shipped in [server/ludics/articulationLattice.ts](../../../../server/ludics/articulationLattice.ts) (`ComputeArticulationJoinResult` at L136–164 with `joinIsMinimal`, `closureSteps: 0`, `coneId`, `coneIds[]`, `collidingLoci[]` payload fields). Consumer [server/ludics/synthesisProposalAgent.ts](../../../../server/ludics/synthesisProposalAgent.ts) branches on all three kinds (L118 `cross-cone-rejected` → 422 `CROSS_CONE`; L126 `same-cone-delocation-required` → sentinel result with `delocationType: "locus-addition-required"`; L142 invariant guard on `closureSteps !== 0`). Tests T1–T4b in `phase2c-synthesis-workflow.test.ts` cover `same-cone-join` (T1–T4) + `cross-cone-rejected` (T4b); `same-cone-delocation-required` is in the type as future-proofing but structurally unreachable under current `loci: string[]` literal-set union (no collisions possible). No code edits this round; spec-review row B1 + §5 stale-on-arrival entry annotated as resolved above. |
| **B8** — `proposeSynthesis` discriminated result | Bin 1 | DONE (Round 6, 2026-05-21) | [server/ludics/synthesisProposalAgent.ts](../../../../server/ludics/synthesisProposalAgent.ts) `SynthesisProposalResult` rewritten from a single flat interface (`witnessId: string \| null`, `delocationType`, `wasNontrivial`) to a discriminated union by `kind`, mirroring B1 and per [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §3.3 verbatim: (1) `same-cone-join` with non-null `witnessId`, `joinDesignId`, `newLoci`, `closureSteps: 0` (literal type); (2) `same-cone-delocation-required` with `joinDesignId`, `newLoci`, `delocationCandidateLocus` (no `witnessId` field); (3) `cross-cone-rejected` with `reason: "cross-cone-incompatibility"`, `cone1DesignId`, `cone2DesignId` — **returned as a value with HTTP 200**, not thrown (was previously `throw SynthesisError("CROSS_CONE", …, 422)`; UI at [app/test/ai-epistemic/page.tsx](../../../../app/test/ai-epistemic/page.tsx) L968 already documented the 200-with-kind expectation). `SynthesisErrorCode` reduced from 5 codes to 4: dropped `CROSS_CONE` (now a kind), renamed `DELOCATION_REQUIRED` → `ROOT_LOCUS_MISSING` (the throw was for the I1 root-locus guard, not the now-returned delocation kind — name correction). Route handler [app/api/v3/ludics/propose-synthesis/route.ts](../../../../app/api/v3/ludics/propose-synthesis/route.ts) doc-block updated to reflect 200-always-on-substrate-outcomes + new error-code catalogue. MCP tool description in [packages/isonomia-mcp/src/server.ts](../../../../packages/isonomia-mcp/src/server.ts) (`propose_synthesis`, ~L1777) rewritten to enumerate the three kinds. [__tests__/invariants/phase2c-synthesis-workflow.test.ts](../../../../__tests__/invariants/phase2c-synthesis-workflow.test.ts) T5–T8 rewritten as discriminated-shape pins (T5 `same-cone-join`; T6 `same-cone-delocation-required` with `@ts-expect-error` on absent `witnessId`; T7 `cross-cone-rejected` with `@ts-expect-error` on absent `witnessId`/`joinDesignId`; T8 idempotent `same-cone-join`). Fixture description drift cleanup: [eval/ai-epi/corpus/v2/fixtures/synthesis-delocation-db.json](../../../../eval/ai-epi/corpus/v2/fixtures/synthesis-delocation-db.json) + [eval/ai-epi/corpus/v2/manifest.json](../../../../eval/ai-epi/corpus/v2/manifest.json) descriptions updated to retire stale `DELOCATION_REQUIRED` / `wasNontrivial` / `closureSteps: 1` language (the fixture is in fact a same-cone-join with newLoci.length > 0, never the unreachable delocation branch). Aggregate unchanged: 18 suites · 344 tests green. |
| **S2 §6.3** — `closureSteps > 0` policy paragraph (OQ-6) | Bin 1 | DONE (Round 6, 2026-05-22) | Paragraph-level doc edit ratifying the policy already enforced in code by B8. [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §6.3 policy box flipped from "soft alert (warn + surface), upgrade to hard error after one production cycle" to "**hard error**, ratified post-B8" with explicit pointer to the `SynthesisError("CLOSURE_STEPS_INVARIANT", …, 422)` throw in [server/ludics/synthesisProposalAgent.ts](../../../../server/ludics/synthesisProposalAgent.ts) L191–L195. Rationale: under Reading A, within-cone ∨_⊥⊥ is the literal chronicle-set union; any nonzero `closureSteps` is a substrate-invariant violation rather than a load/perf signal, so silently continuing would let downstream consumers act on an incoherent join. The WARN log in `articulationLattice.ts` remains for observability. §7 OQ-6 marked CLOSED with strikethrough on the original three-way choice and a pointer back to the §6.3 policy box. No code change this round (the throw shipped with B8). |
| **S2 §1.2** — seed script cone-locality | Bin 1 | DONE (verified Round 6, 2026-05-22) | Verification round, not a code rewrite: [scripts/seed-ludics-showcase.ts](../../../../scripts/seed-ludics-showcase.ts) already creates the post-2e/2f three-design multi-cone B (cone_0 base + cone_0 extension + cone_1 base, |Inc(B)| = 2) per the corrected spec text in [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §1.2 Step 3 (L101–L120), and the `DesignInclusion` insertion at L226–L252 of the script is gated by an explicit cone-locality assertion: `sameCone = derivedBy !== null && isSubset(baseCone0.loci, extensionCone0.loci) && !isSubset(baseCone1.loci, extensionCone0.loci)`; failure throws `"[seed] Cone-locality assertion failed: refusing to insert cross-cone DesignInclusion edge."` (most-specific-base-ancestor rule from Phase 2e). Header doc comment (L1–L29) already documents the multi-cone construction + cross-cone refusal; spec text at §1.2 Step 4 already carries the matching `*[CORRECTED post-2e/2f]*` annotation. A8.2 ("silent on cone-locality") resolved: both the spec section and the runnable seed script are now explicit and enforced. No code edits this round. |
| **T7** — `prisma db push` tooling | Bin 2 | DONE (verified Round 7, 2026-05-22) | Verification round, not a code rewrite: [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) was corrected post-review at all three migration-command sites — §1.2 Step 1 (L86–L91, explicit `"Repo convention… is prisma db push, **not** prisma migrate dev"` callout), §4.4 land-the-schema-first paragraph (L645), §6.5 fingerprint-history migration (L913) — each tagged `*[CORRECTED post-review]*`. Repo convention from [.github/copilot-instructions.md](../../../../.github/copilot-instructions.md) and [AGENTS.md](../../../../AGENTS.md) is now consistent with spec. A8.1 (`F substrate / repo-convention violation`) resolved. No code edits this round. |
| **B3** — `WitnessRecord` tagged retraction columns | Bin 2 | DONE (verified Round 7, 2026-05-22) | Verification round: the Pass B recommendation (`AUGMENT — split into tagged columns, preserve T4`) had already shipped silently across schema, service, and route layers. [lib/models/schema.prisma](../../../../lib/models/schema.prisma) L10290–L10291 carries the split `retractLayer String?  // "argument_superseded"\|"locus_deleted"\|"design_excised"\|"manual_retract"` + `retractReason String?  // free-text reason from the caller`; spec [LUDICS_SESSION_1_DEV_SPEC.md](./LUDICS_SESSION_1_DEV_SPEC.md) §4.1 (L929–L933) documents the split with the `*CORRECTED post-review (Tier 3.11)*` annotation; [server/ludics/witnessRecord.ts](../../../../server/ludics/witnessRecord.ts) (L38–L45) exports the canonical `RetractLayer` union type with `@deprecated RetractReason = RetractLayer` alias for one-release compat, and `fossilizeWitness(retractLayer, retractReason?)` (L112–L113) takes both as separate parameters; [server/ludics/fossilRecord.ts](../../../../server/ludics/fossilRecord.ts) (L28–L30, L117–L118) projects both fields back through `get_fossil_record`; UI surface at [app/test/ai-epistemic/page.tsx](../../../../app/test/ai-epistemic/page.tsx) L969 advertises `"fossilizedAt + retractLayer + retractReason (free-text)"`. A6.1 + A11.5 ("layer collapse baked into schema") resolved end-to-end. No code edits this round. |
| **B2** — `LudicMove.stratumLabel` verify-on-read | Bin 2 | DONE (Round 7, 2026-05-22) | Pass B Alt-4 recommendation (`stored cache + cheap version check + recompute on mismatch`) shipped as a point-lookup helper. New module [server/ludics/stratum.ts](../../../../server/ludics/stratum.ts) exports `getStratumLabel(ludicMoveId): Promise<StratumLabel \| null>` that fetches the move + all deliberation peers + active `WitnessRecord` rows in 3 round-trips, recomputes the authoritative label (walked iff active witness; else witnessable iff parent/child of a walked locus at depth 1; else latent), compares to the stored `stratumLabel` column, emits a structured `console.warn(JSON.stringify({ event: "substrate_violation", kind: "stratum_label_drift", deliberationId, ludicMoveId, locus, stored, authoritative }))` on mismatch, and returns the authoritative value. Aggregate read paths ([server/ludics/exposureMap.ts](../../../../server/ludics/exposureMap.ts) L155–L188, [server/ludics/deliberationSchema.ts](../../../../server/ludics/deliberationSchema.ts) L130–L142) were already deriving `walked` from `WitnessRecord` rather than trusting `stratumLabel`; this new helper is the point-lookup analogue for single-move stratum reads. Spec [LUDICS_SESSION_1_DEV_SPEC.md](./LUDICS_SESSION_1_DEV_SPEC.md) §4.1 NOTE *[ADDED post-review, Tier 3.10]* (L897–L905) already prescribed exactly this contract; helper now realises it. New pin file [__tests__/invariants/phase1d-stratum-verify-on-read.test.ts](../../../../__tests__/invariants/phase1d-stratum-verify-on-read.test.ts) — 7 tests covering: missing-id null return, walked-when-witnessed, witnessable-when-parent-of-walked, witnessable-when-child-of-walked, latent-when-non-adjacent, WARN on stored-vs-authoritative mismatch, no-WARN on match. Aggregate: 19 suites · 351 tests green (up from 18 / 344). |
| **B11** — compound rate-limit key (v2) | Bin 2 | DEFERRED-v2 (verified Round 8, 2026-05-22) | Verification round: the v2 deferral is fully encoded in spec text. [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §6.1 L800 already carries the `> **v2 TODO (B11)** *[ADDED post-review]*` block specifying the compound `(tenant, participant) + (tenant, IP)` key with separate token-bucket adapters and the cross-participant-abuse rationale (one tenant burning another's quota); tracked as OQ-5 in §7 of this review. v1 single-key rate-limit by `participantId` ships per spec; v2 rewrite to compound key remains queued. No code edits this round per the explicit v2-deferral framing of B11 (Pass B winner Alt-2 20/25 vs. spec 19/25 tie — the weakest Pass B Δ in the table, justifying defer). |
| **B6** — scoped session tokens (v2) | Bin 2 | DEFERRED-v2 (verified Round 8, 2026-05-22) | Verification round: the v2 deferral is fully encoded in spec text. [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §6.4 L880 already carries the `> **v2 TODO (B6)** *[ADDED post-review]*` block specifying scoped JWT tokens bound to `(deliberationId, participantId)` with short TTL to eliminate the server-side session lookup; tracked as OQ-7 in §7 of this review. v1 shared bearer + caller `participantId` ships per [server/ludics/auth.ts](../../../../server/ludics/auth.ts) (per §6.4 of the spec); v2 rewrite to scoped session tokens remains queued. The SPEC_REVIEW row header lists "S1 §6 / S2 §6.4" but S1 §6 is `Implementation Ordering`, not an auth section — the canonical site is S2 §6.4 alone, which is where the v2 TODO already lives. No code edits this round. |
| **B12** — drop L1; Redis-only fingerprint cache (v2) | Bin 2 | SPEC-ALIGNED / CODE-DEFERRED-v2 (Round 8, 2026-05-22) | Mixed status: the spec has already been corrected to Alt-2 ([LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §6.5 L891+ titled `*[CORRECTED post-review]*` — prescribes dropping the module-level `Map<hash, CacheEntry>`, single Redis Upstash read-through over a durable `BriefingFingerprintHistory` Postgres record), but the corresponding code rewrite at [server/ludics/briefingFingerprint.ts](../../../../server/ludics/briefingFingerprint.ts) still carries the v1 in-memory caches (`const hashCache = new Map<string, CacheEntry>()` L107, `const deliberationHistory = new Map<string, HistoryEntry[]>()` L117, with reads/writes at L325/L337/L389). The header doc-comment at L17–L18 still describes the v1 "module-level in-memory cache" intent. Code rewrite (drop both Maps, route through Redis + `BriefingFingerprintHistory`) is explicitly **deferred to the v2 production-readiness sprint** per the user's prioritization ("v2 deferrals B11/B12/B6" — Round 7 prompt). Honest framing: A13.5 (cross-instance L1 coherence) remains a known v1 operational risk that the deployment topology must mitigate (single-instance deployment ⇒ no coherence violation; horizontally scaled ⇒ staleness possible). Tracked as OQ-6b for the v2 sprint. No code edits this round. |
| **A8.5** — stratum recompute / neighbour relabel | Bin 2 | DONE (closed-by-B2 + Test 14, Round 8, 2026-05-22) | Two independent mitigations now in place. (1) **Recompute primitive shipped (Round 7 / B2).** [server/ludics/stratum.ts](../../../../server/ludics/stratum.ts) `getStratumLabel(ludicMoveId)` recomputes the authoritative stratum from `WitnessRecord` + tree adjacency and WARN-logs `substrate_violation/stratum_label_drift` on mismatch; this is the exact recompute σ(𝓟_P)^⊥ primitive that the original A8.5 finding ("Seed `stratumLabel = "walked"` static — derived state from σ(𝓟_P)^⊥, no test for neighbour relabeling") asked for. (2) **Neighbour-relabel test pinned at spec level.** [LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §1.3 Test 14 *[ADDED post-review]* (L168) and the mirrored Test 14 at [LUDICS_SESSION_1_DEV_SPEC.md](./LUDICS_SESSION_1_DEV_SPEC.md) L1100 already specify: "After a `bind_participant_to_design` call promotes a `latent` move to `walked`, the `stratumLabel` of neighbour moves at depth ≤ `stratifyDepth` is re-derived from σ(𝓟_P)^⊥ … seed three latent neighbours, bind one, assert the other two are relabeled `witnessable`." The B2 helper subsumes the recompute side of A8.5 (point-lookup verify-on-read); the spec’d Test 14 fixture is the write-side complement (relabel-on-write integration test). UC severity downgraded to F. No code edits this round. |
| **A9.4** — `incarnation-conflation` misstatement kind | Bin 2 carryover | DONE (closed under Option A, Round 8, 2026-05-22) | A9.4's first half — `miscount-across-cones` — shipped Round 6 with B7's per-cone semantics cascade (`ConfidentMisstatementKind` gained `"miscount-across-cones"`; `T16b` pins the single-cone guard in [__tests__/invariants/phase2b-incarnation-manifest.test.ts](../../../../__tests__/invariants/phase2b-incarnation-manifest.test.ts)). The second half — `incarnation-conflation` (agent treats two incomparable incarnations as the same incarnation) — closed without code edits under the **"no abstractions without a witnessed instance" discipline**. Rationale: every concrete consumer error pattern that initially motivated `incarnation-conflation` already has a natural scorecard home — (a) fusing two cones' loci into one set is caught by `miscount-across-cones` (`claimedIncarnationCount = 1` vs. manifest ≥ 2); (b) picking one cone's minimal and labelling it "the bottom" is caught by per-cone `meanRecall` collapse (other cones score 0); (c) the only genuinely uncovered pattern — agent asserting equality between two incomparable incarnations — requires a new `BriefingClaim` field (e.g. `claimedIncarnationEquivalences: Array<[id, id]>`) before there is anything to score against, and no current consumer surface emits that claim shape. Closing this would re-open the moment either (i) MCP exposes cone identity via the Alt-3 forward-compat `IncarnationSetManifest.cones?` slot (currently optional, unused by mock-client) or (ii) a prompt template asks the agent to declare equivalences between articulations. Both are tracked in §7 (MCP cone-identity OQ) so a future trigger has a documented re-entry path. No code or test edits this round. |
| **A11.3** — manual-retract endpoint announcement integration | Bin 2 | DONE (Round 9, 2026-05-22) | Closed under the **v1-stub form already specified in the dev-spec** ([LUDICS_SESSION_2_DEV_SPEC.md](./LUDICS_SESSION_2_DEV_SPEC.md) §4.5 *[ADDED post-review]*): the manual-retract endpoint now emits a single `console.info({ event: "witness_rescinded", witnessId, reason: "manual_retract", fossilizedAt })` on fresh retract; the v2 swap to a real A1–A4 announcement bus remains tracked as OQ-4 in §7. Round 8's carryover note ("deferred pending the announcement-bus design") was over-cautious — the spec had already carved out the v1 surface. Shipped together with the related §4.5 *[CORRECTED post-review]* idempotence drift that was still in the route: [app/api/v3/ludics/retract-witness/route.ts](../../../../app/api/v3/ludics/retract-witness/route.ts) previously returned `409 ALREADY_FOSSILIZED` on a repeat retract, contradicting the function-layer idempotence and the corrected spec; now returns `200 { ok: true, alreadyFossilized: true }` instead. The announcement emission is suppressed on the idempotent path (the rescission already fired on the original retract). Test pins: T13 swapped from a 409 assertion to a 200-with-`alreadyFossilized: true` assertion (+ asserts no fresh `update()` call); new T13b spies on `console.info` to assert the `witness_rescinded` payload shape on fresh retract and confirms no re-emit on the idempotent repeat — both in [__tests__/invariants/phase2d-fossil-lifecycle.test.ts](../../../../__tests__/invariants/phase2d-fossil-lifecycle.test.ts). |
| **B12 (v2 closure)** — Redis-only fingerprint cache | Bin 2 carryover | DONE (Round 10, 2026-05-23 — WS-1 of LUDICS v2 sprint) | The v2-deferred code rewrite landed. [server/ludics/briefingFingerprint.ts](../../../../server/ludics/briefingFingerprint.ts) refactored to a 3-tier read-through: **L0** bounded in-proc `Map` (cap 256, test-only `__resetBriefingFingerprintL0()` for invalidation in Jest) · **L1** Upstash REST cache (TTL 300s) · **L2** durable `BriefingFingerprintHistory` Postgres truth. Write order is L2 → L1 → L0; reads backfill upper tiers on hit. The two legacy module-level `Map`s (`hashCache`, `deliberationHistory`) are gone; A13.5 cross-instance L1 coherence risk is closed (Redis is the now the cross-pod source-of-truth and L0 capacity is bounded). L2 writes are guarded by `if (!p.briefingFingerprintHistory) return;` + try/catch so suites that mock Prisma without the delegate do not blow up. New pin tests in [__tests__/invariants/phase2f-production-readiness.test.ts](../../../../__tests__/invariants/phase2f-production-readiness.test.ts) T1–T4 (cold-miss L2 write · warm-hit hash stability · `recordFingerprint` rotation with R1 attribution · horizontal-scale L2 rehydration after L0 reset). OQ-6b CLOSED. |
| **B11 (v2 closure)** — compound rate-limit key | Bin 2 carryover | DONE (Round 10, 2026-05-23 — WS-2 of LUDICS v2 sprint) | The v2-deferred compound key landed. `BindErrorCode` widened with `"RATE_LIMITED"`; `BindError` ctor takes an optional 4th `retryAfter?: number`; `BindInput` accepts optional `ip?: string \| null`. [server/ludics/bindParticipantToDesign.ts](../../../../server/ludics/bindParticipantToDesign.ts) calls `compoundRateLimit({ scopeId: deliberationId, participantId, ip, action: "bind" }, { perParticipant: 10/min, perIp: 30/min })` immediately after the canonical `deliberationId` is resolved. Route adapters forward `x-forwarded-for` and emit `Retry-After` header + `retryAfter` body on `RATE_LIMITED`: [app/api/v3/ludics/bind-witness/route.ts](../../../../app/api/v3/ludics/bind-witness/route.ts) (action `"bind"`) + [app/api/v3/ludics/propose-synthesis/route.ts](../../../../app/api/v3/ludics/propose-synthesis/route.ts) (action `"propose_synthesis"`). Per WS-0 audit the compound key uses `scopeId = deliberationId` rather than the originally-spec'd `tenantId` (no tenant axis exists in schema). Test pins T5–T10 in [__tests__/invariants/phase2f-production-readiness.test.ts](../../../../__tests__/invariants/phase2f-production-readiness.test.ts) (participant cap · IP cap across participants · cross-deliberation isolation · `Retry-After` integer math · window rollover via `Date.now` patch · missing-IP graceful skip) — runs fully in-memory (Upstash env vars deleted in `beforeEach`). `__resetMemoryRateLimits()` added to phase1b `beforeEach` to prevent bucket bleed. OQ-5 CLOSED. |
| **B6 (v2 closure)** — scoped session tokens | Bin 2 carryover | DONE (Round 10, 2026-05-23 — WS-3 of LUDICS v2 sprint) | The v2-deferred scoped JWT landed. New [server/ludics/auth.ts](../../../../server/ludics/auth.ts) is the single auth primitive: `issueScopedToken({ deliberationId, participantId, ttlSeconds=3600 }) → JWT(HS256, jose)`; `verifyScopedToken(jwt, { requireDeliberationId? })` maps `jose` failures to `LudicsAuthError { code, status }` (`INVALID_TOKEN` 401 · `EXPIRED_TOKEN` 401 · `SCOPE_MISMATCH` 403); `enforceTokenScope(caller, deliberationId)` is a no-op unless `authMode==="jwt"`; `resolveLudicsCaller(req)` resolution order is **bearer → JWT** (when `LUDICS_JWT_SIGNING_KEY` is set) **→ legacy `MCP_API_TOKEN` bearer** (only when `LUDICS_LEGACY_BEARER==="1"`, with a `console.warn` deprecation log) **→ session fallback** via `getCurrentUserId()`. Per WS-0 the JWT carries `scopeId = deliberationId`, NOT a tenant claim. Ops CLI [scripts/mintMcpToken.ts](../../../../scripts/mintMcpToken.ts) added (`npx tsx scripts/mintMcpToken.ts --deliberation <id> --participant <id> [--ttl 3600]`). 7 `/api/v3/ludics/*` routes migrated off inline `resolveCallerUserId`; 4 enforce scope (`propose-synthesis`, `unwitnessed-exposure`, `bind-witness`, `fossil-record`), 3 deferred for lack of `deliberationId` in API surface (`witnesses`, `instantiation`, `retract-witness`). `BindWitnessSchema` extended with optional `deliberationId` to allow opt-in scope enforcement. New env vars in [.env.example](../../../../.env.example): `LUDICS_JWT_SIGNING_KEY` (required), `LUDICS_JWT_ISSUER` (default `mesh-ludics`), `LUDICS_LEGACY_BEARER` (default `0`, transition-only). Test pins: dedicated [__tests__/invariants/phase2f-ws3-scoped-jwt.test.ts](../../../../__tests__/invariants/phase2f-ws3-scoped-jwt.test.ts) (11 cases — T11–T15 + session fallback) plus the route-level T11–T15 in [phase2f-production-readiness.test.ts](../../../../__tests__/invariants/phase2f-production-readiness.test.ts) (happy 200 · scope mismatch 403 · expired 401 · legacy off 401 · legacy on 200 + warn). Jest config required `transformIgnorePatterns: ["node_modules/(?!(jose)/)"]` because `jose@6` is ESM-only. OQ-7 CLOSED. |
| **§6.6** — production-readiness invariant suite | n/a (new spec deliverable) | DONE (Round 10, 2026-05-23 — WS-4 of LUDICS v2 sprint) | New pin file [__tests__/invariants/phase2f-production-readiness.test.ts](../../../../__tests__/invariants/phase2f-production-readiness.test.ts) — 17 grouped tests + 1 `it.todo` placeholder, covering all three perimeter workstreams under one suite: T1–T4 fingerprint cache (B12 / WS-1), T5–T10 compound rate-limit (B11 / WS-2), T11–T15 scoped JWT (B6 / WS-3), T16 cache-warmer (`it.todo` — `WARM_LUDICS_CACHE_JOB` not yet implemented), T17 OQ-5b `evaluateStalenessRules` priority ordering (R1 > R3 > R5; null on no-change). Infra gotcha pinned in [/memories/repo/ludics-v2-ws4-2026-05-22.md](../../../../) — `derivePrioritizedOpenCqs` is called **synchronously** inside `extractMaterialFields`; mocks MUST use `.mockReturnValue([])` not `.mockResolvedValue([])`. |
| **A1–A4 (OQ-4 closure)** — announcement bus | Bin 2 carryover | DONE (Round 10, 2026-05-23 — WS-5 of LUDICS v2 sprint) | The v1 `console.info` stub from A11.3 is now backed by a real BullMQ-fanned, Postgres-durable announcement bus. Protocol doc landed at [LUDICS_ANNOUNCEMENT_BUS_PROTOCOL.md](./LUDICS_ANNOUNCEMENT_BUS_PROTOCOL.md) (v0.2 Accepted — BullMQ transport; audit-log-only subscriber for v0; 8 explicit deferred TODOs in §7.1 for UI/MCP/webhook push, sweep cron, audit-split, A3 emit, console.info removal, Streams upgrade). New Prisma model `SubstrateAnnouncement` at [lib/models/schema.prisma](../../../../lib/models/schema.prisma) end-of-file with idempotency triple `@@unique([eventType, subjectId, occurredAt])` named `substrate_announcement_idem`; pushed to Supabase via `npx prisma db push` (per repo convention — not `migrate dev`). New library [lib/ludics/announcementBus.ts](../../../../lib/ludics/announcementBus.ts) exports `AnnouncementEnvelope` Zod schema + `publishAnnouncement()` (DB-insert-first, then BullMQ `queue.add`; P2002 swallowed → `{ ok: true, deduped: true }`) + `defaultJobOpts()` (`attempts: 5`, exponential backoff `1000ms`, `removeOnComplete/Fail: 1000`). Critical lazy queue init via dynamic `import("bullmq")` + `import("@/lib/queue")` so test suites do not need `UPSTASH_REDIS_URL`. New worker [workers/ludics/announcementDispatcher.ts](../../../../workers/ludics/announcementDispatcher.ts) (queue name `"substrate-announcement"`, concurrency 4) implements the v0 audit-log subscriber inline + exports `replayUndelivered(olderThanMs=5*60*1000)` for the future sweep cron (TODO[BUS.SWEEP-CRON]); self-registers via `import "@/workers/ludics/announcementDispatcher"` in [workers/index.ts](../../../../workers/index.ts). Three emit sites wired (all wrapped in try/catch per protocol §5.3 — bus failures MUST NOT fail the user-facing op): A1 `witness_committed` in [server/ludics/bindParticipantToDesign.ts](../../../../server/ludics/bindParticipantToDesign.ts) (post-`$transaction`); A2 `design_revealed` in [server/ludics/synthesisProposalAgent.ts](../../../../server/ludics/synthesisProposalAgent.ts) on the **fresh-commit branch only** (NOT the idempotent `existing` short-circuit at ~L249) — `occurredAt = witness.timestamp.toISOString()` to keep idempotency stable across re-emits; A4 `witness_rescinded` in [app/api/v3/ludics/retract-witness/route.ts](../../../../app/api/v3/ludics/retract-witness/route.ts) as a **DUAL EMIT** (`console.info` retained alongside `publishAnnouncement` until v2.5 cutover per TODO[BUS.AUDIT-CONSOLE-REMOVAL]) to regression-proof phase2d T13/T13b. A3 `witness_contested` deferred as TODO[BUS.A3-EMIT] (challenge layer not yet shipped). Per WS-0 the envelope carries `scopeId = deliberationId`, NOT a tenant claim. New pin file [__tests__/invariants/phase2g-announcement-bus.test.ts](../../../../__tests__/invariants/phase2g-announcement-bus.test.ts) — 12 tests: T1–T3 envelope Zod shape · T4 DB persist (asserts `occurredAt` is `Date`) · T5 P2002 dedupe → `{ ok: true, deduped: true }` · T6 BullMQ enqueue with `attempts: 5` · T7 idempotent re-publish · T8 `replayUndelivered` where-clause · T9 `defaultJobOpts` shape · T11 A4 retract route calls `publishAnnouncement` · T12 A4 retract route STILL calls `console.info` (dual-emit regression-proof) · T13 alreadyFossilized branch does NOT publish. `caller.callerId` (NOT `caller.participantId` — `LudicsCaller` shape on `server/ludics/auth.ts:49`) is used as `actorParticipantId`. OQ-4 CLOSED for v0; UI/MCP/webhook subscribers remain TODO per §7.1 of the protocol. |

**Design note on B5 — additive vs. replace.** The Pass B recommendation reads `AUGMENT — { hubHash, refusalHash, claimGraphHash, ... }`. Two implementations satisfy that contract:
- **Additive (chosen).** Keep `contentHash` byte-stable; add `components` alongside it. Pin the consistency invariant in tests: any `contentHash` change ⇒ ≥ 1 component digest change.
- **Replace.** Redefine `contentHash = sha256(`${hubs}|${frontier}|${refusal}|${witnessing}`)`. Cleaner single source of truth, but invalidates every pinned `manifest.contentHash` in `eval/ai-epi/snapshot/corpus/v2/`.

The additive path was chosen to preserve the corpus pins and ship without a fixture-regeneration sub-task. The replace path remains available if a future consumer surface needs the combined-hash framing directly.

**Design note on B4 — mostly already shipped.** The cost of B4 turned out to be one Prisma column verification + four doc-comment edits, not the full mechanical rewrite the original Pass B Alt-2 (25/25) anticipated. The substrate-canonical names had been introduced in earlier rounds without a coordinated terminology PR; this round retired the last visible legacy labels (I1–I4 in test headers and the MCP tool description). Public API responses, DB column values, and the live `bindParticipantToDesign` contract all already used S1–S4 + `daimon` at the start of this round.

**Test surface deltas (cumulative through Round 5):**
- `__tests__/invariants/phase1f-fingerprint-components.test.ts` — new (B5, Round 4).
- `__tests__/invariants/phase1f-r1r5-coverage.test.ts` — new (A8.4, Round 5).
- `__tests__/invariants/phase2d-fossil-lifecycle.test.ts` — T15.2 added (B9 defensive structural-where).
- `__tests__/invariants/phase1g-scorecard-manifest.test.ts` — fixture-count assertion bumped 5 → 7 (pre-existing drift surfaced while running B5 regression; `synthesis-join-db` + `synthesis-delocation-db` had been added since the baseline assertion was written).
- `__tests__/invariants/phase1b-bind-seam.test.ts` — header + section-comment + 1 `it(…)` title renamed (B4, Round 5); no behavioural change.
- `server/ludics/bindParticipantToDesign.ts`, `app/api/v3/ludics/bind-witness/route.ts`, `packages/isonomia-mcp/src/server.ts` — doc-comment-only edits (B4, Round 5); no behavioural change. `packages/isonomia-mcp/dist/server.js` not hand-edited (regenerates from src).

**Aggregate at landing (Round 9):** 19 suites · 352 tests green across `__tests__/invariants` + `__tests__/eval` (+1 from Round 8: new T13b `witness_rescinded` emit-and-suppress test; T13 swapped semantics 409 → idempotent-200 without changing the test count).

**Aggregate at landing (Round 10 — v2 production-readiness sprint complete, 2026-05-23):** 22 suites · 393 tests + 1 `it.todo` green. Delta from Round 9: WS-3 added [phase2f-ws3-scoped-jwt.test.ts](../../../../__tests__/invariants/phase2f-ws3-scoped-jwt.test.ts) (+11); WS-4 added [phase2f-production-readiness.test.ts](../../../../__tests__/invariants/phase2f-production-readiness.test.ts) (+17 tests + 1 todo); WS-5 added [phase2g-announcement-bus.test.ts](../../../../__tests__/invariants/phase2g-announcement-bus.test.ts) (+12); WS-1/WS-2 are exercised through the WS-4 suite rather than dedicated pin files. Command: `npx jest __tests__/invariants __tests__/eval`.

**Aggregate at landing (Round 11 — v2.5 cutover complete, 2026-05-24):** 22 suites · 390 tests + 1 `it.todo` green (down 3 from Round 10). Cutover scope: (1) A4 retract route `console.info({event:"witness_rescinded",…})` dual-emit removed from [app/api/v3/ludics/retract-witness/route.ts](../../../../app/api/v3/ludics/retract-witness/route.ts) — bus publish is now the sole substrate channel (LUDICS_ANNOUNCEMENT_BUS_PROTOCOL.md §7.0 / TODO[BUS.AUDIT-CONSOLE-REMOVAL] closed); (2) legacy `MCP_API_TOKEN` bearer fallback gated by `LUDICS_LEGACY_BEARER=1` removed from [server/ludics/auth.ts](../../../../server/ludics/auth.ts) — the resolution order is now (i) scoped JWT, (ii) session cookie; the JWT-failure `catch` now re-throws `LudicsAuthError` with its precise code (`SCOPE_MISMATCH` / `EXPIRED_TOKEN` / `INVALID_TOKEN`) instead of collapsing to `INVALID_TOKEN` to allow the legacy fallback; `LudicsAuthMode` union narrowed `"jwt" | "legacy" | "session"` → `"jwt" | "session"`; `LUDICS_LEGACY_BEARER` removed from `.env.example` (the standalone `MCP_API_TOKEN` env var stays — non-ludics surfaces `lib/citation/mcpAuth.ts` and the isonomia MCP server still consume it). Test deltas: `phase2d-fossil-lifecycle` Manual-retract describe migrated off the legacy bearer to `resolveLudicsCaller` + `publishAnnouncement` mocks via `jest.doMock` (mirrors phase2g T11/T12 pattern); T13b assertion swapped from a `console.info` spy to a `publishAnnouncement` bus-mock assertion. `phase2g` T12 inverted from "STILL calls console.info" → "does NOT call console.info". `phase2f-production-readiness` T13 now asserts `EXPIRED_TOKEN` (was `INVALID_TOKEN`) and T14 recast as "any non-JWT bearer → 401"; T15 ("legacy on → 200 + warn") deleted (−1). `phase2f-ws3-scoped-jwt` T14 similarly recast; T15 describe block deleted (−2 tests); the `enforceTokenScope` no-op test dropped its `legacy` half. Command: `npx jest __tests__/invariants __tests__/eval`.

**Bin 2 remaining (0 items).** Bin 2 closed 2026-05-22 with A11.3 (manual-retract announcement v1-stub + §4.5 idempotence drift). All Tier-1 review findings are now either landed in code/tests or explicitly deferred to v2 with a documented re-entry path in §7. (B11/B6/B12 code remain v2-deferred per the dev-spec's own production-readiness tags; A9.4 `incarnation-conflation` closed Round 8 under "no abstractions without a witnessed instance"; A8.5 closed-by-B2 via [server/ludics/stratum.ts](../../../../server/ludics/stratum.ts) recompute primitive + spec'd Test 14 neighbour-relabel fixture; A11.3 closed Round 9 via the v1-stub `console.info` emission + 409 → idempotent-200 fix in [app/api/v3/ludics/retract-witness/route.ts](../../../../app/api/v3/ludics/retract-witness/route.ts).)

**Bin 1 remaining (0 items).** Bin 1 closed 2026-05-22 with S2 §6.3 paragraph rewrite + S2 §1.2 seed-script verification. (B4, S2 §5 archive shipped Round 5; B7 manifest rewrite + B1 join-algorithm ratification + B8 `proposeSynthesis` discriminated kinds + S2 §6.3 hard-error ratification + S2 §1.2 seed-script verification all logged in §9 — see above.)

---

*End of review. See [LUDICS_SESSIONS_1_2_SPEC_REVIEW_WORKING_NOTES.md](LUDICS_SESSIONS_1_2_SPEC_REVIEW_WORKING_NOTES.md) for full per-round evidence and per-finding scoring detail. Companion spec ↔ code audit is the separate [LUDICS_SESSIONS_1_2_AUDIT_PROMPT.md](LUDICS_SESSIONS_1_2_AUDIT_PROMPT.md) (not executed by this review).*
